e1 — wire ANTIGO, gravado antes do rename de hoje
=================================================
WIRE COMPLETO (40 bytes):
'#TCF.8 :data-iso\n*600~1,3,1,1,1|\\739617\n'

HEADER
  '#TCF.8'                       magic single-col
  ':data-iso'                    spec `data-iso`

CORPO (2 linha(s))
  [0] '*600~1,3,1,1,1|\\739617'
      -> seq-RLE PERIODICO (ADR-0040): 600 linhas cujo delta CICLA em [1, 3, 1, 1, 1] (periodo 5), ancoradas em '\\739617'. O ciclo paga UMA vez — custo O(1) no numero de linhas
  [1] ''
  nota: o `\` antes dos digitos e' o escape do nucleo (OBAT) para run de digitos — grafia interna, nao faz parte do ordinal

DECODE SEM AJUDA (o que acontece hoje):
  ValueError: nature-id desconhecido no header single-col: 'data-iso' — registry core fechado; forneca o spec out-of-band para decodificar (wire historico: dataclasses.replace(SPEC, wire_id=<id do header>))

DECODE COM A VALVULA out-of-band:
  decode(w, nature=dataclasses.replace(SPEC_DATA_ISO, wire_id='data-iso'))
  -> 600 valores, iguais a' entrada: True

E' a decisao 3 do ADR-0041 (resolucao ESTRITA): o passado se le' pelo git,
nao por bagagem no codigo. Os 14 wires historicos do repo sao TODOS single-col,
logo TODOS alcancaveis por esta valvula.
