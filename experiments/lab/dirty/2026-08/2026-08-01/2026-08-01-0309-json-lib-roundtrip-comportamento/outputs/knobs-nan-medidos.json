{
  "dumps(nan) default": "[NaN]",
  "loads default": [
    NaN
  ],
  "dumps allow_nan=False": "ValueError: Out of range float values are not JSON compliant: nan",
  "loads parse_constant rejeita": "ValueError: NaN"
}
