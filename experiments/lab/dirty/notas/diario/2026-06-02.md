# 2026-06-02 — receita-cnpj real (T-DATA-2) + reorg separacao de concerns (Fases 0-7)

> **ENTRADA RETROATIVA** (reconstruída 2026-07-09, T-CLEAN-3 T3-a) a partir dos commits do dia
> (git log) + README.methodology.md §13.2 (plano brownfield), old/tcf/LEVELS-REVIEW.md e
> tickets/T-DATA-2-RECEITA-CNPJ.md. Escrita na chave da ÉPOCA — sem conhecimento posterior.

## O dia

- Madrugada: `setup_receita_cnpj.py` com logica de parse/montagem/validacao PROVADA contra
  zip sintetico no formato real da Receita (30-col `;`-sep latin-1 sem header); download
  real bloqueado do sandbox — owner roda na rede BR (525a11b).
- Noite: filename deixou de ser adivinhado — descoberta via autoindex + `--list` (1ab4999),
  motivado por 404 na rede do owner. Em seguida, descoberta da API real: Receita migrou pra
  Nextcloud WebDAV; script reescrito com streaming-stop (raw-inflate, para apos N linhas,
  nao baixa os ~2GB) (f7ded09).
- Dataset **receita-cnpj** REAL non-PII: 200k estabelecimentos, contraparte ecologica do
  br-identidades sintetico. MEDICAO em CNPJ real (10k): nature='cnpj' 64.1% vs M10 puro
  108.4% = **ganho 40.9%** (>> gate 5%), 100% compressible sob SPEC_CNPJ, RT 100%. 1a fonte
  ECOLOGICA de check-digit → nature CNPJ **confirmada-empirica** (confianca Media, falta
  N>=5 fontes). T-DATA-2 closed-done (f7ded09).
- **Reorg separacao de concerns** — plano brownfield 8 fases (§13.2), assessment aprovado
  pelo owner etapa a etapa; fronteira-alvo `llm-benchmark/`:
  - Fase 0+3 (5a15538): doc-fixes README/MAP (claims falsos removidos, Quickstart v0.5
    cercado como historico) + `old/tcf/LEVELS-REVIEW.md` — semantica autoritativa L0-L3
    derivada do codigo (L1=RLE, L2=sort+RLE, L3=dict+sort+RLE; 'schema-only' NAO
    implementado). Chesterton's fence pra absorver-e-apagar depois.
  - Fase 1 (c09889a): secao "REORG em andamento" no CLAUDE.md (invariantes: src/tcf
    intocado, git mv, nao mover reversibility.json) + desambiguacao schema core vs gadget
    (4 `schema.py` no repo — nunca editar por basename).
  - Fase 2 (6c08abe): deleta `__main__.py` quebrado (importava run_matrix.py ausente);
    `benchmark_llm_accuracy.py` arquivado morto SEM conserto (importa nomes de old/tcf).
    Suite 280 passed + 1 xfailed.
  - Fase 4+5 fundidas (45f03ad): git mv atomico do harness LLM v0.5 → `llm-benchmark/`
    (53 arquivos de experiments/eval + 3 scripts/benchmark_*); results/scratch gitignored
    ficam em experiments/; `phase0/reversibility.json` NAO movido (artefato de formato v0.6).
  - Fase 6 (0b66c5e): decisao do owner — findings **FICAM em docs/** (13 links relativos
    `../` quebrariam; fronteira por NATUREZA do artefato: catalogo vs codigo).
  - Fase 7 (bb02cff): README enxuto 332→184 linhas; core v0.6 separado do LLM v0.5 (29
    links resolvem). **Fases 0-7 completas; src/tcf INTOCADO em todas.**

## Fontes

- git log 2026-06-02 (9 commits: 525a11b, 1ab4999, f7ded09, 5a15538, c09889a, 6c08abe,
  45f03ad, 0b66c5e, bb02cff) + README.methodology.md §13.2 + old/tcf/LEVELS-REVIEW.md.
