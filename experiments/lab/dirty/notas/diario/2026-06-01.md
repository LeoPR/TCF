# 2026-06-01 — br-identidades sintetico + samples part/partsupp (T-DATA)

> **ENTRADA RETROATIVA** (reconstruída 2026-07-09, T-CLEAN-3 T3-a) a partir dos commits do dia
> (git log) + corpos de commit, tickets T-DATA-2/3/4 (criados no dia) e
> datasets/canonical/br-identidades/README.md. Escrita na chave da ÉPOCA — sem conhecimento posterior.

## O dia

- Dataset **br-identidades** adicionado (f5e2fa8): SINTETICO declared-bias — 500k pessoas
  (CPF) + 100k empresas (CNPJ), reusando geografia real do ibge-municipios. Identificadores
  100% validos (mod-11 via SPEC_CPF/SPEC_CNPJ welded), mascarados, stored como string
  (zeros a esquerda preservados); `socio_cpf` = FK real cross-table (30133 rows, 0 orfas).
  Exercita natures opt-in (ADR-0015) + gate de ativacao.
- Verificado: 500k/100k unicos, FK OK, amostra 3000 100% compressible + RT lossless sob
  spec welded, 0 all-same-digit, 27 UFs. Vies declarado: **NAO sustenta confirmada-empirica**
  sozinho. Volume >100k → encode direto, fora do regime do shaper.
- Referencia leve no git (metadata + README + samples 100 rows); dado pesado regeneravel
  em Z: via `setup_br_identidades.py` + `csv_to_sqlite.py`.
- 3 tickets de planejamento registrados (f5e2fa8), origem no design workflow de taxonomia
  de tipos de dados: **T-DATA-2-RECEITA-CNPJ** (caminho real nao-PII pra fechar o gate de
  generalizacao das natures), **T-DATA-3-EDGE-QUALITY-FIXTURES** (dados de borda pra
  gadgets de qualidade, detect-never-fix, deferred), **T-DATA-4-TPCH-PART-SAMPLES**.
- **T-DATA-4 closed-done** (c9b4984): 4 samples part/partsupp (sf001 + sf01, 100 rows
  cada), fatia direta das primeiras linhas do CSV externo em Z:, sem download. Torna
  p_type (hierarquia), p_brand, p_mfgr, p_container observaveis dos samples git-tracked
  (antes metadata-deep).

## Fontes

- git log 2026-06-01 (2 commits: f5e2fa8, c9b4984) + tickets/T-DATA-{2,3,4}-*.md e
  datasets/canonical/br-identidades/ (introduzidos nos proprios commits do dia).
