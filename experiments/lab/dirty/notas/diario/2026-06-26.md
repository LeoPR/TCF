# 2026-06-26 — Fila #TCF.8 + T-CI-3 (gate Cython) + estratégia de distribuição + ADR-0030

> **ENTRADA RETROATIVA** (reconstruída 2026-07-09, T-CLEAN-3 T3-a) a partir dos commits do dia
> (git log) + `fila-estudos-tcf8.md`, `estrategia-distribuicao-cython.md`, ADR-0030 (docs da era,
> datados 2026-06-25). Escrita na chave da ÉPOCA — sem conhecimento posterior.

## O dia
- **Fila de estudos #TCF.8 consolidada** (ecd263e): varredura read-only (registry + tickets +
  futuras + sessão) → 3 baldes: (A) acionável — congelar single-col, EI global, T-CI-3, GDICT B2;
  (B) estudar antes — modo-1/ISO-date, H-REF-03/02, T-DATA-1; (C) reservado — Pacote 7 natures,
  H-INTRA, LOSS, EI serializado. Achado: **GDICT-01 B1 é o único que já cruzou o gate real-world**
  (−19.3% SNAP); H-NAT-MARK-01 parado (gate ≥15%/2-reais não bate). Ordem: T-CI-3 → congelar
  single-col → EI global → GDICT B2 → H-REF-03 → T-DATA-1.
- **T-CI-3 em dois passos**: outputs de inspeção DRAFT (7e834d9) — ambiente compila o `.pyx`
  (Cython 3.2.5 + MSVC); 138 gates byte-canonical passam com accel=True; Cython vs pure-Python
  = 0 diff em 31 datasets. Owner aprovou o enabler de 1 linha (salva `_detect_compositions_py`
  antes do override, byte-neutro) → gate vira teste (8def19c): `tests/test_pyx_byte_equivalence.py`
  (31 sintéticos + 3 real-world + 8 aleatórios; 42 passed com accel=True, skip se accel=False;
  suite 456). Fecha o buraco: a suite normal só rodava pure-Python. T-CI-3 segue in-progress
  (falta job CI que compile a extensão).
- **Estratégia de distribuição do acelerador Cython** (140a280, `estrategia-distribuicao-cython.md`,
  práticas verificadas na web em junho 2026): compilação NÃO se embute no runtime (dica monkeypatch
  + fallback pure-Python); **cibuildwheel** (uv frontend) pra wheels por plataforma, com T-CI-3
  rodando no build de cada wheel. Forward-looking: free-threading 3.14t+ (`detect.pyx` é função
  pura → thread-safe por construção), abi3/abi3t (PEP 803/809), meson-python/maturin se o
  compilado crescer. Implementação do cibuildwheel fica pro release (0.8.0).
- **ADR-0030** (64f6875): congela o body single-col no 1.0 — gramática HCC `^ * , ~ .. \` +
  OBAT+HCC+seq-RLE + LF/UTF-8 viram contrato imutável → órfão (body-only, sem shebang) decodável
  pra sempre como "TCF 1.x single-col". Linchpin do órfão default do ADR-0029. Disciplina:
  otimização futura do body = desvio opt-in MARCADO (#TCF.8), nunca mutação silenciosa. Janela:
  efeito no 1.0; pré-1.0 ainda refinável (ADR-0024). Itens 1-2 da ordem da fila fechados no dia.

## Fontes
- git log 2026-06-26 (5 commits: ecd263e, 7e834d9, 8def19c, 140a280, 64f6875) +
  `experiments/lab/dirty/notas/fila-estudos-tcf8.md`,
  `experiments/lab/dirty/notas/estrategia-distribuicao-cython.md`,
  `docs/adr/0030-freeze-single-col-body-at-1.0.md` (versões da era via git show).
