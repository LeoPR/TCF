# 2026-06-27 — EI encostado; design B2 group-dict; re-segmentação em 5 workstreams

> **ENTRADA RETROATIVA** (reconstruída 2026-07-09, T-CLEAN-3 T3-a) a partir dos commits do dia
> (git log) + `resegmentacao-workstreams-2026-06-27.md`, `design-b2.md`/`design-b2-revisao.md`
> (datados 2026-06-27). Escrita na chave da ÉPOCA — sem conhecimento posterior. Sessão contínua
> da madrugada (00:14–03:36), continuação de 06-26.

## O dia
- **EI (escape-invertido): medição de alcance do flip global → ENCOSTADO** (fc228d3). 3 achados:
  (1) filtro cego pós-encode nem round-trip fecha — escape-de-literal mistura com dígitos
  estruturais (refs OBAT, contadores `*N`); EI exige ser estágio DENTRO do encoder; (2) em textual
  real só paga em coluna incompressível (CPF 21%, decimal 18%, telefone 15%, uuid 13%);
  (3) sob brotli evapora ou inverte (CPF −19.4%). Alcance estreito (nicho textual/lazy
  random-digit); estágios 0-3 documentados; retomar pelo Estágio 1 só se uso textual-puro aparecer.
- **Design B2 — group-dict híbrido V2 opt-in #TCF.8** (d986816, zero-código, `src/tcf` intocado;
  weld é B3 sob aprovação): estende o `@dict` do V2-B de per-column pra grupo hoistado. Núcleo =
  particionamento greedy custo-modelado (a dobradiça do B1: pool sse dedup > custo de largura);
  namespace por grupo bound à largura; decode = prelúdio serial (dict 1×) + colunas; default-off
  byte-idêntico.
- **Revisão adversarial do B2 na sessão** (workflow 6 lentes × verify cético): 35 achados →
  **24 confirmados (0 blocker, 2 major), 11 refutados**. Núcleo sólido (greedy, reuso V2-B,
  discriminador sobrevivem aos ataques); defeitos concentrados no wire-format framing (#TCF.8M
  não tem header multi-linha; tab contém `\n`/`=`/`,`) e na honestidade de gate/prosa →
  **correções A-D identificadas, aplicação pendente** (próximo passo registrado).
- **Re-segmentação em 5 workstreams** (2d8d429, owner confirmou): W1 cross-dict same-domain (B2) |
  W2 binarização por nature (NOVO, 8 bools/byte, V2-L) | W3 decode paralelo + lazy multi-col |
  W4 TCFL linguagem de spec (compilador F1 já existe; expressivo deferido 2.0, ressalva no
  filtros-dsl-plano §F5) | W5 posicionamento value-prop. Chave: **W1≠W2** — cross-dict de s/n dá
  ≤4.6% textual e regride sob brotli; o ganho de s/n é binarização, não share. Impacto no B2:
  estreita a same-domain puro; **gate geral N≥5 fontes reais** (owner); **brotli/gzip = CONTROLE
  padronizado** (sempre reportar, nunca gate pass/fail).
- **Bibliografia curada por workstream** (dcb9bbd): survey W1-W4 + verify anti-alucinação → 24 refs
  recomendadas, 24 verificadas reais, 0 rejeitadas, em `docs/reference/bibliografia.md`. O repo só
  citava 2 das 24 (Roaring, FOR) → sub-referenciado. Não há paper-library em Z: (PDFs = corpus
  pdf2md, sem relação).

## Fontes
- git log 2026-06-27 (4 commits: fc228d3, d986816, 2d8d429, dcb9bbd) +
  `experiments/lab/dirty/notas/resegmentacao-workstreams-2026-06-27.md`,
  `experiments/lab/dirty/2026-06-21-gdict-caracterizacao/design-b2.md` e `design-b2-revisao.md`
  (datados 2026-06-27; a re-segmentação já referencia as correções A-D da revisão).
