# 2026-07-07 — bN por largura de bits + consolidação com correções + meta-grupo de tipos

> **ENTRADA RETROATIVA** (reconstruída 2026-07-09, T-CLEAN-3 T3-a) a partir dos commits do dia
> (git log) + labs dirty 2026-07-07-* + notas tipos-como-specs.md (seção CONSOLIDAÇÃO) /
> tipos-meta-grupo-fluxo.md + roadmap H-TYPE. Escrita na chave da ÉPOCA — sem conhecimento posterior.

## O dia

- **spec_bin Formato A vs B** (8e05b67, lab 0016): o HCC já produz o binário como literais+refs
  (^1=bit0, ^2=bit1); Formato A (streaming, reusa o stream de refs do HCC) preferido sobre B
  (2 passadas); spec_bin = camada pós-HCC (V2-L), não substituto. RT-OK.
- **Generalização bN** (7bc3114, lab 0028): spec primitivo por largura de bits — k distintos → w bits
  → 8/w linhas/byte (k≤2 b, k≤4 b2, k≤16 b4, k≤256 b8). Spec = `col:b<w>` + lista do domínio
  EMBUTIDA = a referência; spec_bin = caso b. Medido em 12 colunas reais (adult/tpch), RT-OK.
  Pesa vs HCC-nativo (owner): HCC-RLE ganha ordenado (textual/explicável); bit-pack ganha espalhado
  (V2-L); motor escolhe o menor; header `col:b<w>` roteia.
- **Consolidação do espectro com 2 CORREÇÕES** (5401b02), após pesquisa de prior-art pedida pelo
  owner (ADR-0018 V2-L + ponto cego baixa-card; ADR-0025 V2-B já welded; H-REF-05 já previa "sumir
  sob brotli"):
  1. **baseline errado**: os 3 labs de bit-packing (2332/2354/0028) mediram contra encode single-col
     que ignora `fallback`; baseline correto = V2-B já weldado → ganhos caem de 16×/6-9×/2-6× pro
     padrão teórico limpo **8/w** (8× k=2, 4× k=3-4, 2× k=5-16), 12 colunas reais.
  2. **colapso sob brotli**: ganho pré-brotli (2-8×) vira **1.01–1.33× pós** (q11, 4 colunas) —
     confirma H-REF-05. Escopo honesto: TCF como representação TERMINAL (sem re-compressão a jusante).
  Não é welding candidate nesta forma (N<5 fontes + gate brotli reprovado). Correções em APPEND, sem
  reescrever histórico (3 labs + tipos-como-specs + roadmap H-TYPE-02/03 + dict-referencia + ADR-0018).
- **Meta-grupo de tipos** (a924e35): fecha H-TYPE-00..04 + taxonomia QUANDO o tipo é identificado
  (ENTRADA declarado / PROCESSO induzido no pre-pass / PÓS-HCC bN no min() por-coluna), aterrada em
  encoder.py+core.py. bN = IRMÃO bit-packed do dict/V2-B (mesmo domínio+índices; radix bits vs
  base-94) — explica o colapso sob brotli. DOIS contratos de header distintos: multi-col flat =
  char-PREFIXO; TCF.8H = sufixo `col:size`/`:tipo`/`col:b<w>`. Nota tipos-meta-grupo-fluxo.md.
- **Protótipo bN como candidato do min()** (119c2d4, lab 2138): par bn_encode/bn_decode (body
  auto-descritivo [w][domlen][dom][packed]) + mini-container min(tcf,raw,bN) por coluna com marcador
  char-prefixo `#`. RT-OK. Real adult: bN vence baixa-card (sex/race/education/age); k>256 nem se
  oferece (min fica com raw). Gate: 5.39× pré-brotli, 1.42× pós → candidato opt-in (saída terminal).
  CAVEAT: min() sem o V2-B real SUPERESTIMA — prova o MECANISMO, não a margem. Gated H-TYPE-02/03;
  H-TYPE-04 registrada como projetada.

## Fontes

- git log 2026-07-07 (5 commits: 8e05b67, 7bc3114, 5401b02, a924e35, 119c2d4) + labs dirty
  `2026-07-07-{0016-spec-bin-formato-A-B,0028-spec-bitwidth-bN,2138-bn-candidato-min-prototipo}` +
  notas tipos-como-specs.md (CONSOLIDAÇÃO) / tipos-meta-grupo-fluxo.md + roadmap-hipoteses.md
  (H-TYPE-00..04) + T-OPT-INFERENCE.md + ADR-0018 (update pointer).
