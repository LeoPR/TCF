# Diario 2026-06-15 — Fechamento do ciclo 0.7 (higiene + decisoes)

Sessao de revisao/fechamento. Owner: "revisar as atividades e ver prioridades
pra fechar o que pudermos do 0.7". Bytes-core do 0.7 ja' estava welded — o
fechamento foi em boa parte higiene de tickets + 2 decisoes do owner.

## Inventario (workflow)

Workflow `_wf_close_07.js` (16 itens abertos avaliados em paralelo + sintese,
17 agentes). Saida estruturada classificou cada item: belongs (core/adjacente/
v2.0/gadget) + disposition (close-done / small-finish / needs-owner-decision /
defer-v2.0 / park) + effort + value. Sintese confirmou: o 0.7 (bytes) esta'
fechado; resta higiene + 4 decisoes do owner.

## Decisoes do owner (2026-06-15)

1. **Lossy**: o **0.7 permanece lossless-puro**. V2-C round (nicho ~1.5%, so'
   wine) NAO entra; fica como caracterizacao de referencia. Pacote 10 (loss
   amplo) vira roadmap v2.0; se perseguido, **cross-coluna primeiro** (H-LOSS-02,
   `valor=soma(parcelas)`), nao round simples, sob GATE N>=5 + meta-camada de
   contrato (H-LOSS-00).
2. **Nome PyPI**: `tcf-format` (mantendo `import tcf`).
3. **Higiene**: fechar tudo (so' docs/tickets/pyproject; `src/tcf` intocado).

## Executado (docs/tickets/pyproject — zero codigo)

- **pyproject.toml**: `name` `tcf` -> `tcf-format`; `version` `1.0.0` -> `0.7.0`
  (alinha ADR-0024 + CITATION.cff + `__init__.__version__`, que ja' era 0.7.0);
  auto-ref do meta-grupo `all` corrigida pra `tcf-format[...]`. Verificado antes:
  nada le `importlib.metadata.version("tcf")`; `packages=["src/tcf"]` desacopla
  nome-da-distribuicao do pacote importavel. Renomear e' seguro.
- **ADR-0018**: `proposed` -> `accepted` (referencia do roadmap de formato).
  Bloco de estado: V2-A/B/split = DONE; V2-D = REFUTADO; V2-C = caracterizado/
  nao-welded (decisao lossless); V2-J/K/L = defer v2.0.
- **Tickets fechados** (fase welded DONE, fases restantes parked): T-CODE-ENCODER
  -MANAGER (1+1b), T-CODE-SCHEMA-BUILDER (1+2), T-CODE-LAYERED-PIPELINE (Fase 1).
- **Tickets parked** (`deferred` + rationale): T-CODE-OUTPUT-SINKS, T-CODE-PLAN
  -CONTRACT, T-SHAPER-CODE-HARDENING, T-RECOVER-LLM-SCHEMA-MODE (spin-off),
  META-TYPE-ENCODERS (execucao T02-T07, espera T-DATA-1).
- **T-DIST-PYPI-NAME**: `blocked` no upload do owner; nome+pyproject registrados.
- **roadmap-hipoteses.md**: Pacote 10 ganha a decisao de escopo (lossless-puro 0.7).
- **STATUS.md**: bloco unico "FECHAMENTO DO CICLO 0.7".
- **Ja'-prontos confirmados** (so' refletidos no quadro): T-FIX-SHAPER-STRATIFY
  -TEST, T-H-PERF-06-V2-T01-WELD-15, T-H-PERF-06-V2-T02-CYTHON (ja' closed-done).

## Pendente (acao do owner)

- Reservar/publicar `tcf-format` no PyPI (credenciais).
- Lossy (Pacote 10) so' reabre com nova decisao explicita do owner (v2.0).
- Follow-up tecnico adiado: V2-B RLE no stream (incremento <5% estimado).
