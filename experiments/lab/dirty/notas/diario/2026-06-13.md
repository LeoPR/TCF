# Diário — 2026-06-13

## Análise de organização de `src/tcf/` + registro bibliográfico in-code [probatório]

Pedido do owner: avaliar se o código-núcleo precisa reorganização pra
padronizar narrativa/rastreabilidade ("registro bibliográfico do código",
código-como-documento). Análise crítica read-only via workflow multi-agente
(6 leitores por subsistema + 2 cross-checkers de rastreabilidade ADR/spec +
síntese + 2 críticos adversariais: anti-churn §9 vs anti-complacência §5).

**Veredito: NÃO reorganizar a estrutura.** 22 arquivos, layout saudável.
- Rastreabilidade ADR↔código exemplar e bidirecional (9 ADRs, byte-fiel).
- Naming saudável (`auto_*` heurística; único descasamento `syntax.py`/
  `M8AVirtualRefsSyntax` é byte-canonical justificado).
- Mover pré-pass→`prepass/` ou renomear `core/`→`obat/` /
  `composicional/`→`compositional/` = custo de import churn + re-pin de ~15
  artefatos + risco de GATE, benefício cosmético. Desproporcional (§9).

**Gap real (in-code, §5)** — os dois críticos se contradisseram sobre §5 e a
reconciliação definiu a ação:
1. Módulos cravavam **números falsificáveis** (`1523B`, `322B`, `-11.73%`,
   `-33.02%`, `99.5%`) sem apontar in-code o teste que os pina. As % nem são
   pinadas como literal (confirmado por grep em tests/) — só os *bytes* estão
   (`test_real_world_snapshots.py`). Claim probatório na altitude errada.
2. Folhas do pré-pass não declaravam a própria camada — leitor a frio
   precisava do CLAUDE.md externo pra saber que são CAMADA-0.

**Aplicado (docstring-only, zero churn de import/API/byte):**
- **Pacote 0** — orientação de camada nas 4 folhas pré-pass (`auto_cadence`,
  `auto_min_len`, `column_features`, `obat_shape`): declaram CAMADA-0 + ordem
  do pipeline + irmãos.
- **Pacote 1** — ponteiro pro guardião onde há número falsificável
  (`__init__.py`, `auto_min_len.py`, `encoder.py`, `decoder.py`,
  `composicional/syntax.py`, `hcc_seqrle.py`): cada número → seu teste real
  (bindings verificados por grep antes de citar, p/ não apontar nome que
  apodrece — preocupação do crítico anti-churn). `syntax.py` agora nomeia
  HCC + CAMADA-2 + GATE (era o pior caso de vocabulário/altitude: liderava
  por "M8.A").
- **Pacote 2** — back-pointer `output-convention.md` em `decoder.py`,
  `hcc_seqrle.py`, `syntax.py`.
- **Pacote 3 (meta-correção)** — os leitores do workflow marcaram
  `points_to_tests: true` pra módulos pré-pass; era **falso** (citavam path
  de *experimento*, não teste; "D1-D9" no `auto_min_len` é gating `n>=100`,
  não ponteiro de teste). Registrado aqui pra não dar o gap por fechado
  erroneamente. Após o Pacote 1, o flag passa a ser verdadeiro de fato.

**Rejeitado por proporcionalidade:** mover/renomear pasta; colar "ver test_X"
em massa nos 22 módulos (anti-pattern de fonte-dupla — o mapa código→teste
mantém casa primária no CLAUDE.md/GATE; o in-code é "see also" cirúrgico só
onde há claim falsificável).

**Prova:** diff 9 arquivos +53/−4, 100% docstring/prosa, zero lógica. Suíte
`320 passed, 1 xfailed` (idêntico ao baseline) — invariantes byte-canonical
intactos.

## Faxina de cruft + verificação da convenção Z: [probatório]

Varredura de arquivos suspeitos/obsoletos (workflow `varredura-obsoletos-src`,
7 agentes read-only) + execução de limpeza aprovada pelo owner (A+B+C).

**Verificação independente antes de apagar** (lição "conferir antes de
apagar"): dos 72 untracked-não-ignorados, **72/72 com conteúdo (blob) já
presente no HEAD do git** (`git hash-object` vs `git ls-tree -r HEAD`). Os
"5 únicos" que os sub-agentes marcaram eram falsos-positivos (conteúdo está
arquivado em `docs/workbench/_archive/` + `dirty/old/Mobsolete/`, ambos
tracked). Nenhuma fonte única em risco.

**Footgun evitado:** o relatório sugeriu `git clean -dXf` pra Categoria 1.
Dry-run mostrou que varreria `config/storage.json` (resolve paths do Z:!) +
`config/api_keys.json` + `.venv`. Limpeza feita **dirigida** (rm explícito de
cache/build + `git clean -dXf` ESCOPADO a `src/` e ao lab Cython, onde não há
config). Verificação crítica pós-limpeza: **nenhum arquivo rastreado deletado**.

**Executado:** (A) cache/build regenerável — 12 `__pycache__`, `src/build/`,
`detect.{c,pyd}`, build do lab Cython; (B) 72 duplicatas untracked; (C) `tmp/`
descartado (conteúdo já em ADR-0020 + `docs/theory/h-perf-06-exploration.md`).
Suíte `320 passed, 1 xfailed` **com o acelerador compilado removido** — prova
o fallback pure-Python byte-idêntico.

**Convenção Z: (dev-environment/README.md) verificada:** TCF está 100%
conforme — 6 env vars User-scope → `Z:\caches`, `.venv` junction →
`Z:\venvs\tcf`, pytest `cache_dir` em pyproject.toml. Causa dos `__pycache__`:
**resíduo pré-migração** (anterior a 2026-05-08), não recorrência. Pós-limpeza,
rodar 320 testes recriou **0** `__pycache__` in-tree (bytecode foi pro Z:). A
limpeza gruda. Único cruft que reaparece é `detect.{c,pyd}` em build explícito
(gitignored, fora do escopo da convenção, fallback byte-idêntico — aceito).

## Conexões
- Workflow: `analise-org-src-tcf` (11 agentes); `varredura-obsoletos-src` (7)
- Convenção infra: `C:/Users/leona/OneDrive/Documents/dev-environment/README.md`
  (memória `project_z_drive_python_convention`)
- Princípios: Strata §5 (fonte única por altitude), §9 (proporcionalidade)
- Doutrina aplicada ao próprio código: "a prosa aponta, o teste mede"

## Faxina de organização (6 stales) + abertura da v2.0 + bug de core [probatório]

Continuação da sessão. Owner pediu: alinhar a organização geral + decidir como
continuar os testes. Workflow `tcf-org-test-roadmap` (8 leitores + síntese)
mapeou pontas soltas e o roadmap de teste. Todo achado conferido na fonte
(git/disco/teste) antes de agir — um foi falso-positivo (`experiments/eval/`
existe vazio, não sumiu).

**Org — 6 stales verificados corrigidos** (commit `fb03824`, doc-only): CLAUDE.md
Fase 7 PENDENTE→FEITA (commit bb02cff); STATUS:472 baseline M9 1615B→M10 1523B;
índice ADR parava em 0009→estende 0010-0021; MAP.md experiments/eval→llm-benchmark/eval;
TCF-format.md slug ADR-0008; T-DATA-1 OPEN→CLOSED (3 datasets já baixados).
Memória user (`project_reorg_separation_of_concerns` + MEMORY.md) também
dessincronizada — atualizada. Itens de decisão deixados pro owner: link
`../README.methodology.md` (16 refs, doc-mãe migrou pra `../Methodologies/`);
rótulo v0.6 vs v1.0 (§9: não reescrever em massa).

**Teste — owner escolheu abrir v2.0** (perseguir bytes; ADR-0018 estava `proposed`).
Stage 1 = caracterização expandida de V2-A (fallback identity, prioridade #1):
9 fontes reais, **7.85% weighted**, ganho 0.20-12.48%, RT 9/9 — cumpre o
checklist confirmada-empírica (commit `b1c78c8`).

**Achado dominante: bug de RT no core M10** (mais urgente que bytes). Caracterizar
V2-A em receita-cnpj `nome_fantasia` (free-text real) expôs violação de
`decode(encode(x))==x`. Minimizado e corrigido (commit `d3acf39`, aprovado pelo
owner). Root cause: OBAT tokeniza `''` inconsistente — primeira única `[L('')]`
(conta frag), não-primeira `[]` (não conta); o decode nunca reservava o index →
off-by-one em back-ref posterior. Dois modos: (1) `syntax._parse_decl` reserva
frag vazio só com `prox_idx==0`; (2) `hcc_seqrle.encode` `rstrip('\n')`→`[:-1]`
(vazio final sumia). Fix decode-only/`[:-1]` → byte-canonical-safe (D1-D9=1523B
intacto, bytes M10 idênticos). 12 reproducers pinados; suíte 332 passed.
Disciplina: a 1ª tentativa (reservar incondicional) regrediu retail no gate
real-world — pego pelo gate, refinado com `prox_idx==0`. Gate fez o trabalho.

Ticket `T-CODE-EMPTY-FRAG-INDEX-RT` (CLOSED). Lab `2026-06-13-v2a-fallback-expandido`
(6 scripts). Follow-up não-bloqueante: fixture free-text empty+prefixo no gate
real-world; weld de V2-A (precisava do core correto, agora satisfeito).

## Weld de V2-A — abre a v2.0 (#TCF.7, opt-in) [dispositivo]

Owner: "pode fazer o weld de V2-A". Weldado no core seguindo o padrão do
codebase (capacidade opt-in, default preserva byte-canonical — igual natures
ADR-0015 e layered-pipeline): `encode(table, fallback=True)` ativa; default
`fallback=False` → saída byte-idêntica ao v1.

- **Formato `#TCF.7 M`** (ADR-0022, accepted): por coluna escolhe min(TCF, raw);
  coluna raw marcada `!<size>=<name>` (`!` antes do size — zero colisão com nome,
  melhor que o `!name` do proto). Emite #TCF.7 só quando alguma coluna cai pra
  raw; senão #TCF.6 byte-idêntico. Decoder lê os dois (self-describing).
- **Arquivos**: `multi.py` (encode min + decode dual-magic + `_fallback_safe`),
  `encoder.py` (param `fallback`), `decoder.py` (dispatch #TCF.7). Escopo multi-col
  (single-col não tem header — follow-up).
- **Verificação**: smoke (tabela hour baixa-card: -21.69% com fallback, RT OK;
  no-benefit → #TCF.6; raw com vazios RT OK; decode self-describing) + 8 testes
  `TestV2AFallback`. Suíte **340 passed, 1 xfailed**. Byte-canonical intacto
  (default OFF): D1-D9=1523B, D17a=322B, snapshots real-world verdes.

ADR-0022 accepted; ADR-0018 anotado (V2-A welded, V2-B/C/D seguem roadmap).
**Decisões de release pendentes (owner, não bloqueiam)**: flip do default
fallback on; bump pacote 1.0.0→2.0.0; migration doc v1→v2; fallback single-col.

## Faxina do README + diretriz byte-a-byte + header v2 mínimo (continua 2026-06-14)

**README de abertura reescrito** (commit `3d568c0`): desafio (transmitir com
menos bytes) → exemplo cadastro nos 3 formatos com **bytes reais e saída de
verdade** (JSON 480 / CSV 213 / TCF 182, output byte-exato + legenda) → "o que
é" → OBAT/HCC por propósito. Legibilidade **não super-vendida** (owner: mais
fatora, mais denso, mas ASCII). Resultados honesto: removida a frase errada
"TCF vence 7/9 vs gzip/brotli/zstd" (contradizia a filosofia — `csv+brotli`
1742 < `tcf+brotli` 2141; TCF troca ratio por legibilidade). Stale corrigidos
(suite, ratio, ADRs). 0 links quebrados. Decisão: o exemplo didático "falsificado"
virou saída real do `encode` (bytes justos).

**Diretriz do owner (2026-06-14)**: foco em **compressão byte-a-byte** — "cada
byte importa, principalmente se o TCF for substituir transmissões MINÚSCULAS".
Reframe do §9: em payload pequeno o header fixo domina → micro-opts O(1)/tabela
viram fração relevante. Registrado: memória `project-byte-level-compression-focus`
+ O-FMT-15/16 + bundle "header v2 mínimo".

**Header v2 mínimo welded** (commits `47adc82` + revisão; ADR-0023 accepted):
owner pediu "em seguida já o header mínimo", depois revisou ("revise o cabeçalho
... o parâmetro foi M, a próxima linha já é sobre as colunas, logo até o `#` é
dispensável"). Resultado: `encode(table, min_header=True)` → `#TCF.7 M` + meta
`<s>=<n>,...,<nN>`. **Revisão do header v0.6**: como o flag `M` no shebang já
declara o meta de colunas, **todo `#TCF.7` dispensa o prefixo `# ` inteiro**
(não só o espaço — o `#` também); vale pra fallback e min_header. `#TCF.6`
mantém o `# ` (congelado, ADR-0017, intocado). min_header também omite o size
da ÚLTIMA coluna (corpo até EOF, igual single-col). Default off preserva
byte-canonical. Medido: cadastro README **182→177 B** (−5: `# ` + `20=`);
fallback sozinho 182→180. Suíte **351 passed**. Arquivos: `multi.py` (meta sem
prefixo p/ #TCF.7 + decode v6-exige-`# `/v7-dispensa + size-None→EOF),
`encoder.py` (param `min_header`). 11 testes `TestMinHeaderV2`. O-FMT-15/16 →
WELDED. Decoder tolerante (aceita `# `/`#`/nenhum em v7). Pendente futuro:
deferred-sizing/streaming (O-FMT-08/V2-J).
