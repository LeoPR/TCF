# 2026-07-12 — revisão de fechamento + fila por ROI do 0.8

## Pedido do owner

Organizar as atividades da revisão com o método documental do projeto, temporalidade e
rastreabilidade; usar a memória persistente do Copilot para reler o estado na abertura do projeto;
priorizar o fechamento do núcleo `.8` sem perder bugs e situações de borda.

## Avaliação executada

- Fontes cruzadas: STATUS, ROADMAP, README, CHANGELOG, pyproject, tickets de QA/closeout/release,
  CI/release workflows, decoder, Cython e documentação pública.
- Suíte CI-friendly: **540 passed, 2 skipped, 60 deselected**.
- Suíte completa com os 6 hubs existentes em `Z:/tcf-data/interim/`: **600 passed, 2 skipped**.
- Ruff de `src/tcf scripts tests`: 75 ocorrências; 13 no pacote ativo, predominantemente
  legibilidade/estilo. O job de lint continua não bloqueante.
- Estado Git observado: `main` 10 commits à frente de `origin/main`; nenhum push/tag/publicação.
- Wheel/release: pré-smoke local válido, mas F3/F4/F6 e reconciliação do workflow ainda precedem C3.

## Achado novo

**BUG-14**: round-trip falha para cinco separadores Unicode aceitos pelo encoder (`\v`, `\f`, NEL,
`U+2028`, `U+2029`). Repro 5/5: um valor vira duas linhas porque `splitlines()` no decoder reconhece
mais separadores do que o contrato LF-only. Classificado como domínio válido e gate R0 antes de F3.
Foi corrigido no lote A da mesma data, com registro técnico e gates no bloco abaixo.

## Decisão documental

- `T-REL-08-CLOSEOUT` permanece a **única ordem dispositiva**, agora em ROI R0→R3.
- Núcleo `.8` primeiro: BUG-14 → F3 → F4 → F5 condicional → F6 → C3.
- BUG-12/corrupção e orçamento defensivo ficam rastreados em 0.8.1/pré-1.0 sem sequestrar o release.
- Pesquisa de codec/tipos/quoting/V2-B B/C permanece `.9`.
- Checkpoint criado:
  `checkpoints/2026-07-12-revisao-roi-fechamento-08.md`.
- Memória Copilot de reentrada foi gravada no escopo do repositório, apontando para STATUS,
  T-REL, checkpoint e último diário; ela é ponteiro operacional, não fonte de decisão.

## Aberto para a próxima sessão

1. Executar T-QA-8 F3.
2. Seguir para F4 com os hubs já disponíveis.
3. Manter F5 condicional e seguir a fila R1-R3 sem abrir trabalho `.9`.

## Execução técnica registrada (Lote A)

Fechamento do **BUG-14 (R0)** com rastreabilidade completa em código, teste e ticket.

- Teste-first: adição de casos parametrizados em `tests/test_core_rt.py` cobrindo os 5 separadores
  Unicode no fluxo single+multi (`\v`, `\f`, `U+0085`, `U+2028`, `U+2029`).
- Red comprovado: `uv run pytest tests/test_core_rt.py -k "unicode_line_separators" -q`
  retornou `5 failed, 5 passed`.
- Correção mínima no core: decode LF-only em
  `src/tcf/composicional/syntax.py` e `src/tcf/composicional/hcc_seqrle.py`
  (remoção de `splitlines()`, split exclusivo por `\n`, preservando separadores Unicode como dado).
- Green do bug: mesmo comando retornou `10 passed`.
- Gates pós-fix: `uv run pytest tests/test_core_rt.py tests/test_regression_v1_baseline.py
  tests/test_real_world_snapshots.py -q` retornou `104 passed`.

Rota de continuidade permanece no dispositivo `T-REL-08-CLOSEOUT`: próximo concreto = F3.

## Bloco 2 — revisão do core (diretriz owner) + C0 dedup cirúrgico

Owner (2026-07-12): core "muito espalhado, mesmo que funcional — preciso refazer"; exemplo = o
tratamento do BUG-14 duplicado em hcc_seqrle e syntax ("aumenta absurdamente a chance de problemas
de sincronismo de lógica de negócios"); muitas funções; e **M8A é nome de protótipo** — a
tecnologia foi fixada mas o nome vivo é HCC. "Simplificar o código para poder avançar mais."

Registro: **T-CODE-CORE-CONSOLIDATE** (`b164a2b`) — inventário MEDIDO (4 duplicações D1-D4;
~131 defs em src/tcf, view 35/syntax 21; M8A em 8 arquivos + .pyx) + fases C0-C3 + o princípio
**"dois tempos do fix"** do owner (T1 correto-sujo com ifs → T2 integrado-inteligente; os fixes
F0 são majoritariamente T1 por design e o C2 é o tempo-2 deles, com latência medida antes/depois).

**C0 EXECUTADO** (aprovação owner; byte-neutro por construção):
- D1: `split_lf_body` (composicional/syntax.py) — fonte única do contrato LF-only; os DOIS decodes
  consomem (o fix do BUG-14 não vive mais em 2 lugares).
- D2: `_stringify_checked` (multi/core.py) — stringify+check de \n/\r única pros ramos dict e list.
- D3: `_decode_raw_body` (multi/core.py) — split do body raw único pra decode E view.
- Gate: suíte **610 passed** (inclui os 10 testes novos do BUG-14); pinos exatos 1523/300/89616;
  grep confirma zero cópias restantes. D4 (camada dupla do decode) fica pro C2, pós-release.

Nova ordem proposta (no ticket, aguardando decisão do owner): C0 ✓ → F4-mínimo → F6 → C3 release
→ C1 (rename M8A→HCC) + C2 (achatar decode) como abertura do ciclo seguinte → specs clássicos-BR
pós-C2 (survey de specs ficou parcial — re-rodar 2 agentes quando liberado).

## Bloco 3 — F4-mínimo iniciado (run interrompida por shutdown; retomada trivial)

Driver `scripts/bench_evidencia_f4.py` criado e RODANDO quando o owner precisou desligar:
9 casos (adult-5k, lineitem-5k, customer-full, ibge-full, br-pessoas/empresas ± natures §2.3,
receita ± :cnpj REAL), amostras determinísticas "primeiros 5000", sinal zlib/brotli qualitativo,
NENHUM blob salvo. 4/9 casos tinham completado. O driver LIMPA e regenera `f4-minimo/` a cada
rodada — nada parcial a preservar.

**Retomada (próxima sessão)**: `python -X utf8 scripts/bench_evidencia_f4.py` (~5-10 min) →
conferir RESULT.md → registrar no T-QA-8 F4/CLOSEOUT 2d → commit. Depois: F6 → C3 (go do owner).
Pendências: push (18 commits locais); specs-survey re-rodar 2 agentes; C1/C2 pós-release.

## Bloco 4 — retomada: survey de specs (fechado) + F4-mínimo (re-run)

Owner pediu o status dos specs antes do teste em massa (2 abordagens que ele lembrava).
Survey de 3 agentes (1 re-rodado por resposta degenerada — o re-survey CORRIGIU um erro material
do primeiro). Consolidado em **T-SPEC-STATUS-08**:

- **Abordagem 1 (linguagem generalista / specs induzidas)**: mapeada, confirmada-conceitual,
  gated .9 (T-OPT-INFERENCE + tipos-como-specs + spec_bin/bN, H-TYPE-03 aberto). Nada pro .8.
- **Abordagem 2 (clássicos BR)**: ACHADO central = o gargalo é DADO, não máquina. `check_fn` é
  PARÂMETRO LIVRE do construtor (não dict fechado — o dict fechado é só do gadget DSL); 5 dos 7
  clássicos (CEP/PIS/renavam/título/CNH) cabem sem tocar core, só passando check_fn+body_length;
  telefone (largura variável) e placa (alfanumérico) precisam máquina nova; RG precisa política
  per-estado. MAS: só CNPJ (receita real) fecha o gate ≥15%/2-reais; CPF é synthetic-teto; os
  demais não têm dado real em NENHUM hub e o gerador só faz cpf/cnpj. Único toque de core p/
  spec novo = 1 linha no SPEC_REGISTRY (e id desconhecido AGORA é erro — meu BUG-13b).
- **Decisão pendente do owner**: A (conservador, só CPF/CNPJ no .8 — recomendado) / B (demo de
  capacidade sintética) / C (abrir clássico real, só pós-.8 com dataset). Pré-reqs registrados:
  anonimizador §2.3 + gerador estendido não existem.

**F4-mínimo**: re-executando (run nova, PID vivo); 4/9 casos ao registrar (br-pessoas 5k é pesado).
Ao fechar: RESULT.md + registro T-QA-8 F4 / CLOSEOUT 2d + commit.

## Bloco 5 — F4-mínimo FECHADO (9/9 RT) + achado forte nas natures

Driver `bench_evidencia_f4.py` completou: 9 casos nos hubs prontos, RT 9/9 + determinístico,
material em `evidencia-0.8/f4-minimo/`. Δ vs CSV: adult 81.1%, ibge 68.5%, receita-real 62.4%,
lineitem-free-text 50.2%, br-empresas+cnpj 55.3%. zlib9 sempre menor no TCF; brotli indisponível
no venv (sonda F0-3 registrou ausência graciosamente).

**ACHADO FORTE (repro, cruza com T-SPEC-STATUS-08)**: a nature CNPJ **piora** em dado REAL a nível
de tabela — receita 100121B → 107460B (+7339B) com `:cnpj`; a coluna cnpj cai de `split` (32665B)
pra `raw` (39999B) porque o corpo base-94 da nature destrói a estrutura (matriz/filial, prefixos)
que o split/dict já explorava. No sintético (br-empresas) a MESMA nature ajuda. É o gap
sintético-vs-real (anti-incidente 2026-05-21) medido — nenhum clássico, nem CNPJ, é ganho de
tabela garantido em real → reforça a Opção A do T-SPEC-STATUS-08 + caveat obrigatório do F6.

Registros: T-QA-8 F4-1/3 done, F4-2/4 = janela dedicada; CLOSEOUT 2d marcado; T-SPEC-STATUS-08
ganhou a seção de evidência nova.

## Aberto pra próxima sessão

- **Decisão do owner nos specs (A/B/C)** antes de qualquer spec novo no material.
- **F6** (README/docs com números MEDIDOS + tabela-mestra + nota Wohlin + caveat da nature CNPJ) → C3 release (go do owner).
- C1/C2 (rename M8A→HCC + achatar decode) pós-release; população total do F4 = janela dedicada.
- Push (22 commits locais).

## Bloco 6 — decisão dos specs: OPÇÃO A (owner)

Owner decidiu **A**: só CPF/CNPJ (já welded) no `.8`, com o caveat medido no F4 (nature CNPJ piora
a tabela em dado real). NENHUM spec novo no `.8` — zero core novo, zero risco no tail. Clássicos
que cabem por construtor (CEP/PIS/renavam/título/CNH) e os que precisam máquina nova
(telefone/RG/placa) → `.9`, só com dataset real. T-SPEC-STATUS-08 fechado `closed-decided`; linha
FILTROS-POPULARES do ROADMAP cruzada com a memória (.9 + achado do F4 + pré-reqs). O caveat entra
no F6 como critério de aceite.

Próximo: F6 (docs/README com números medidos + caveat) → C3 release (go do owner). Push pendente.

## Bloco 7 — investigação de fundo dos specs (redirect do owner) → T-SPEC-DEEPDIVE-08

Workflow de 4 agentes (cpf-modelo, cnpj-além, mapa-compressível, compilador-unweld). INSIGHT que
unifica: a nature é pré-transformação camada-0 FORÇADA (encoder.py:277-303) — pré-empta o pipeline
em vez de COMPETIR no min(tcf,raw,dict,split). O corpo base-94 destrói a estrutura inter-linha
(matriz/filial/deltas) que split/dict já exploravam → por isso PIORA em real ordenado e AJUDA em
embaralhado/sintético. "CNPJ básico" = trata 14 díg como 1 inteiro.

Medições novas (receita real, cnpj): filial 97.6%='0001', base-8 94.9% ordenada com deltas +11=37%,
DV 100% derivável; field-split chegaria a ~16.2KB vs 32678 split (~50%). CPF-modelo = 10 eixos
(gabarito de spec bem-estudado). Mapa valida Opção A: nenhum ganho de BYTE novo no .8 (welded já
cobre tudo byte-relevante); bN/bitmap = aceleração/V2-L; GDICT/H-INTRA = .9. Compilador natures_
compiler JÁ é data-driven (regenera CPF/CNPJ/IP byte-idênticos, 14 testes); "tirar do welded" =
registry carregável (build_registry) + namespace core:/user: reconciliando o BUG-13b — pré-1.0.

**CRUX pro owner (T-SPEC-DEEPDIVE §5.1)**: a "nature compete" (fix de maior ROI, recupera +7339B,
safe-by-construction) entra no 0.8 ou 0.8.1? Toca core (encoder+multi fallback), byte-safe, não
move pinos. Recomendação: 0.8.1 (o .8 leva só o caveat), salvo o owner querer agora com gate.

Registros: T-SPEC-DEEPDIVE-08 criado + indexado; F6 herda caveat+nota-compilador; §5 lista as
decisões. Próximo: decisão do owner no §5.1 → F6 → C3.

## Bloco 8 — DECISÃO owner: nature-compete NO .8 + demo medido das intuições

Owner: "quero o fix já no .8; preciso de demonstrações de dados sobre CPF e CNPJ antes; depois
fazer a nature competir; me mostre antes os exemplos com amostras." Registrado em
T-SPEC-DEEPDIVE-08 §5.1 (CRUX decidido: nature-compete no .8, demo PRIMEIRO).

`scripts/spec_demo.py` (medido, emitted_bytes via BUG-07) → `evidencia-0.8/spec-demo/DEMO.md`:
- Transformação de 1 valor: CPF 14B→5B, CNPJ 18B→7B (base-94 descarta máscara+DV).
- **CNPJ real (receita)**: filial '0001'=97.6%, base-8 94.9% ordenada (deltas +11=37%/+10=24%/
  +12=18%), DV derivável. ORDENADA: sem nature 32665B (split) vs com 39999B (raw) = **+7334B**
  (confirma F4). EMBARALHADA: sem 41332B vs com 39999B = **−1333B** (a INVERSÃO provada — mesma
  coluna, só reordenada, o veredito vira).
- **CPF sintético (a intuição nova pedida)**: RANDOM sem nature 7499B vs com 2999B = −4500B
  (ajuda); CLUSTERED sequencial sem nature **1043B (split!)** vs com 2999B = **+1956B** (piora ~3×).
  O CPF tem a MESMA vulnerabilidade — nunca testado com estrutura inter-linha, agora demonstrado.

§2.3 respeitado: CPF efêmero; DEMO.md só mostra placeholders públicos (111/222) + amostras
mascaradas. Próximo: mostrar ao owner → implementar nature-compete (encode 2 modos, fica o menor).

## Bloco 9 — refinamento do owner: nature é matematizável, falta o DELTA (medido)

Owner apontou: na coluna ordenada a nature "apenas codifica" — o corpo é um int, deveria ter o
mesmo benefício do delta; só nunca fizemos um delta pra nature base-94. E o CNPJ /0001 é um código
só (enum), não 4 dígitos — subcódigo posicional (regras Receita Federal).

MEDIDO (CNPJ real ordenado 5000): A) sem nature 32665B (split, baseline); B) nature absoluta hoje
39999B (raw, +7334); **C) nature DELTA (corpo-int deltado) 17032B (dict) — bate o split**;
**D) field-split (base8-delta 14904 | filial-dict 1233 | DV 0) = 16137B**. Ou seja ~2× de ganho
sobre o baseline — não é "recuperar a regressão", é otimização real que passa folgado o gate ≥15%.

Natureza do CNPJ (empírico + regras RF, confirmar oficial): 8 base = inscrição SEQUENCIAL raiz
(ordenada/deltável); 4 filial = contador, 0001=matriz 97.6% (ENUM); 2 DV = mod-11 (0 bits).
Generaliza pro CPF (9º díg região fiscal; lotes com cadência) = gabarito subcódigo posicional.

Registrado em T-SPEC-DEEPDIVE-08 §4-bis. O "fix" agora tem 2 níveis: FLOOR (nature compete, safe,
.8) + CEILING (nature delta-aware/field-decomposed, ~2×, estudo próprio com gate). Decisão de
escopo do owner pendente.

## Bloco 10 — as 3 formas/6 camadas do owner, MEDIDAS (lab spec-camadas)

Owner articulou a arquitetura: spec em 3 formas (A entrada-total = hoje; B paralela = núcleo
trabalha e troca base-94 nas REFERÊNCIAS; C misto = limpa na entrada + troca na saída), ~6 camadas
(limpeza · derivação · pré-forma · núcleo · troca-refs · saída). Lab
`2026-07-12-spec-camadas/` mediu a escada S1-S5 em 4 regimes (CNPJ real ord/shuf, CPF rand/clust):

- **S5 (delta→base94, forma C) é a única sempre-boa**: CNPJ ord 14270B (−56%), shuf 32408B (−22%),
  CPF clust **14B** (−98.7%; delta +3 vira RLE *499|); só perde por pouco no random (3207 vs 2971).
- Forma A (hoje) é a PIOR em 2 de 4 regimes.
- **SURPRESA**: limpeza isolada PIORA o CNPJ (+64%) — a MÁSCARA tem valor estrutural (o split usa
  a pontuação como separador de campos). Camadas não-monotônicas → escolha POR COLUNA, medida →
  reforça o compete-no-min() como chão.
- Máquina: S3/S5 exigem spec ESTATAL por coluna (column-wise nature) — encode_value per-value não
  expressa; capacidade nova a projetar.

Registrado em T-SPEC-DEEPDIVE-08 §4-ter. Escopo pro owner segue: FLOOR (compete, .8) + CEILING
(column-wise nature delta→base94, estudo .9/pré-1.0 — agora com números fortes).

## Bloco 11 — lab spec-camadas v1 refeito COM RIGOR + BUG-15 achado pelo RT counter-proof

Owner cobrou (com razão): o lab anterior não tinha nome-padrão (data/hora/versão), nem artefatos
mostrando o dado (input/debug-fluxo/output/contra-prova), e reportei bytes S3/S5 SEM RT validado —
"só dizer que funciona não é evidência". Refeito: `2026-07-12-1917-spec-camadas-v1/` com README
(metadata), run.py (RT end-to-end por degrau) e artifacts/ numerados (01-input, 02-flow-debug do
núcleo via SideOutputs, 03-output-blobs, 04-rt-counterproof, 05-ladder).

**O RT counter-proof PEGOU UM BUG REAL (BUG-15)**: os blobs de S3/S5 em base-80 NÃO decodam —
literal começando com `^` (marcador de ref do HCC) quebra o RT em modo tcf E dict (raw sobrevive):
`decode(encode(["^abc"]*30+["y"]*30))` → ValueError. O HCC escapa dígito-líder mas não `^`-líder.
Domínio válido (texto com `^`: regex/markup). O alfabeto BASE94 da nature inclui `^` → o CEILING
(nature-delta/field-split) DEPENDE do fix. Registrado T-QA-8 §3 BUG-15 [alta, não-fixado].

Vindicação da disciplina do owner: os números anteriores (17032/14270 base-80, commit 9f152d1)
escondiam o defeito. Refeito em base-62 marker-safe: **RT 20/20 verde**, conclusão sobrevive
(S5 misto sempre-boa: CNPJ ord 14640 −55%, CPF clust 14B; máscara tem valor estrutural).
Blob do CPF clust S5 (14B): '#TCF.8Mc\nRuvdm\n*499|\3' = base + (delta 3 × 499), recon volta aos CPFs.

Suíte 610 passed. Próximo (owner escolheu (b), mais investigação): decidir escopo FLOOR/CEILING
sabendo que o CEILING agora tem uma DEPENDÊNCIA dura (BUG-15).

## Bloco 12 — BUG-15 FIXADO (owner: "fixe o bug-15")

Literal '^'-líder quebrava RT em tcf/dict (crash em não-dígito; corrupção SILENCIOSA em '^12' →
lido como ref pro eid 12). Causa: `_escape_lit` escapava '*'-líder mas não '^'. Fix cirúrgico em
`composicional/syntax.py` `_emit_body`: '^'-líder de linha → '\^' (ref-runs começam com dígito,
nunca '^'; só literal-líder colide); decode de-escapa via `_parse_decl` (mecanismo '\' já existia).

6 repros red→green em test_core_rt.py::TestBug15CaretLeadingLiteral (tcf/dict/dígito-silencioso/
multi-col/só-caret/mid-string-byte-neutro). Verificação extra: cada char do BASE94 como líder → RT
ok. **Byte-NEUTRO**: suíte 616 passed, pinos 1523/300/89616 exatos, Cython inalterado (fix no emit).

Destrava o CEILING dos specs (nature-delta/field-split podia produzir base-94 '^'-líder). Achado
SÓ porque o lab exigiu RT end-to-end — a lição do §RT com bug real capturado.
