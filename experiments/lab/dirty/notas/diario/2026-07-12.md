# 2026-07-12 — revisão de fechamento + fila por ROI do 0.8

## Pedido do owner

Organizar as atividades da revisão com o método documental do projeto, temporalidade e
rastreabilidade; usar a memória persistente do Copilot para reler o estado na abertura do projeto;
priorizar o fechamento do núcleo `.8` sem perder bugs e situações de borda.

## Avaliação executada

- Fontes cruzadas: STATUS, ROADMAP, README, CHANGELOG, pyproject, tickets de QA/closeout/release,
  CI/release workflows, decoder, Cython e documentação pública.
- Suíte CI-friendly: **540 passed, 2 skipped, 60 deselected**.
- Suíte completa com os 6 hubs existentes em `Z:/tcf-data/interim/`: **600 passed, 2 skipped**.
- Ruff de `src/tcf scripts tests`: 75 ocorrências; 13 no pacote ativo, predominantemente
  legibilidade/estilo. O job de lint continua não bloqueante.
- Estado Git observado: `main` 10 commits à frente de `origin/main`; nenhum push/tag/publicação.
- Wheel/release: pré-smoke local válido, mas F3/F4/F6 e reconciliação do workflow ainda precedem C3.

## Achado novo

**BUG-14**: round-trip falha para cinco separadores Unicode aceitos pelo encoder (`\v`, `\f`, NEL,
`U+2028`, `U+2029`). Repro 5/5: um valor vira duas linhas porque `splitlines()` no decoder reconhece
mais separadores do que o contrato LF-only. Classificado como domínio válido e gate R0 antes de F3.
Foi corrigido no lote A da mesma data, com registro técnico e gates no bloco abaixo.

## Decisão documental

- `T-REL-08-CLOSEOUT` permanece a **única ordem dispositiva**, agora em ROI R0→R3.
- Núcleo `.8` primeiro: BUG-14 → F3 → F4 → F5 condicional → F6 → C3.
- BUG-12/corrupção e orçamento defensivo ficam rastreados em 0.8.1/pré-1.0 sem sequestrar o release.
- Pesquisa de codec/tipos/quoting/V2-B B/C permanece `.9`.
- Checkpoint criado:
  `checkpoints/2026-07-12-revisao-roi-fechamento-08.md`.
- Memória Copilot de reentrada foi gravada no escopo do repositório, apontando para STATUS,
  T-REL, checkpoint e último diário; ela é ponteiro operacional, não fonte de decisão.

## Aberto para a próxima sessão

1. Executar T-QA-8 F3.
2. Seguir para F4 com os hubs já disponíveis.
3. Manter F5 condicional e seguir a fila R1-R3 sem abrir trabalho `.9`.

## Execução técnica registrada (Lote A)

Fechamento do **BUG-14 (R0)** com rastreabilidade completa em código, teste e ticket.

- Teste-first: adição de casos parametrizados em `tests/test_core_rt.py` cobrindo os 5 separadores
  Unicode no fluxo single+multi (`\v`, `\f`, `U+0085`, `U+2028`, `U+2029`).
- Red comprovado: `uv run pytest tests/test_core_rt.py -k "unicode_line_separators" -q`
  retornou `5 failed, 5 passed`.
- Correção mínima no core: decode LF-only em
  `src/tcf/composicional/syntax.py` e `src/tcf/composicional/hcc_seqrle.py`
  (remoção de `splitlines()`, split exclusivo por `\n`, preservando separadores Unicode como dado).
- Green do bug: mesmo comando retornou `10 passed`.
- Gates pós-fix: `uv run pytest tests/test_core_rt.py tests/test_regression_v1_baseline.py
  tests/test_real_world_snapshots.py -q` retornou `104 passed`.

Rota de continuidade permanece no dispositivo `T-REL-08-CLOSEOUT`: próximo concreto = F3.
