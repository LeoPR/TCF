# 2026-05-25 — Diagnostico IP fechado (sub-exps 11-14) + ADR-0015 welded + benchmark formats

> **ENTRADA RETROATIVA** (reconstruída 2026-07-09, T-CLEAN-3 T3-a) a partir dos commits do dia
> (git log) + ADR-0015, nota arquitetura-funil-camadas e blocos de sessao do STATUS.md.
> Escrita na chave da ÉPOCA — sem conhecimento posterior.

## O dia
- Sub-exp 11: gating ADR-0010 REFUTADO como culpado do subnet IP — canonical == no_gating byte-identico em n=50..1000; o problema real e' cross-subnet (seq-RLE nao transita entre prefixes). Gating validado em CPF high-entropy, mantido (2a4289e).
- Sub-exp 12: IP variante D (hex 8-char) abandonada como nicho — fica sempre entre B (base94 6c) e C (padded 12d); HCC seq-RLE e' digit-centric, transicao `9`->`a` quebra o digit-run (2a4289e).
- Sub-exp 13: base-aware seq-RLE parcialmente refutada — strategy pattern (BaseAlphabet + SeqRLEEngine, zero `if base == X`) validado como arquitetura, regressao decimal zero, mas ganho hex marginal (-0.88% subnet). Caminho mais barato e' pre-tx padding, nao generalizar HCC. NAO weld (0fbaca8).
- Sub-exp 14: investigacao cross-subnet identifica 2 bugs reais em src/tcf — Bug #1: M8A nao cria atom secundario apos o primeiro; Bug #2: compare_for_seq rejeita deltas {0,0,0,1} (hcc_seqrle.py:88 exige delta uniforme). Fix exige tocar canonical; nao nesta sessao (b329c2b).
- 3 tickets registrados (T-CODE-HCC-MULTI-DELTA-FIX P2, T-CODE-HCC-ATOM-DETECTION-REFINE P2, T-CODE-LAYERED-PIPELINE P3) + nota `arquitetura-funil-camadas-2026-05-24.md` (funil toggleavel por camada + literatura FOR/PFOR-DELTA/Gorilla/VByte) (b7b0853).
- ADR-0015 WELDED: novo package `src/tcf/natures/` com TemplatedCheckedSpec + SPEC_CPF + SPEC_CNPJ, opt-in via `encode(..., nature=)`. Default sem nature preserva M10 byte-canonical (D17a 322B INVARIANT). 176 passed (+21). CPF 50 vals: -64% vs M10 puro (d1164dd).
- Benchmark formats x compression (96 medicoes: 6 datasets x 4 formats x 4 transports): TCF vence 4/6; brotli vence gzip/zstd em 5/6; TCF estrutural + brotli sao ortogonais (schema patterns vs byte n-grams). Perde em D-IP-subnet (bugs do sub-exp 14) e D17a tiny (header overhead) (c55ca49).
- Lesson META do dia: 3 sub-exps consecutivos (11, 13, 14) convergem em "diagnosticar na camada certa antes de generalizar downstream".

## Fontes
- git log 2026-05-25 (6 commits: 2a4289e, 0fbaca8, b329c2b, b7b0853, d1164dd, c55ca49) — subjects + bodies.
- Docs da era: `docs/adr/0015-natures-templated-checked-weld.md`; `notas/arquitetura-funil-camadas-2026-05-24.md`; blocos de sessao no STATUS.md.
