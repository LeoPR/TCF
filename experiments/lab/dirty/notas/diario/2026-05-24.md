# 2026-05-24 — API unificada (ADR-0014) + arranque do arco natures (CPF/CNPJ/IP)

> **ENTRADA RETROATIVA** (reconstruída 2026-07-09, T-CLEAN-3 T3-a) a partir dos commits do dia
> (git log) + bodies dos commits; checkpoint `2026-05-24-sessao-maxima-natures-multi-delta.md`
> (escrito 2026-05-26, cobre a sessao inteira) usado so' pra conferencia — o corpo abaixo se
> restringe aos commits DESTE dia. Escrita na chave da ÉPOCA — sem conhecimento posterior.

## O dia

- **ADR-0014 WELDED**: `encode(list|dict)` unificado (dispatch por tipo; decode roteia pelo shebang `#TCF.6 M`) + SideOutputs como recipiente opcional — reaproveita OBAT log, HCC trace/rede e seq_rle_runs que eram descartados; encode_table/decode_table viram aliases deprecated; suite 96 -> 117; invariants 1523B e D17a 322B preservados (ad5afeb). TCF-format.md expandido com pipeline completo + camadas futuras (078cd11).
- Registro conceitual Pacote 7: sub-naturezas templated/checksummed/lossy catalogadas (TM-CPF/CNPJ/IP4..., LR-*, CP-*) em nota nova + hipoteses H-TM-*/H-LR-* no roadmap + O-FMT-14 header desacoplavel/derivavel (b4281f3).
- T-CODE-ENCODER-MANAGER Fase 1 WELDED: `encode(data, parallel=)` via ProcessPool, byte-identical, speedup 1.23x em orders 15k (d4343b0); Fase 1b work-stealing nao muda o speedup — gargalo e' IPC/spawn no Windows, teto realista ~1.3x com multiprocessing puro Python (1dc9f8a).
- T-CODE-SCHEMA-BUILDER Fase 1+2 WELDED: `build_schema(data)` -> TableSchema/ColumnSchema com zero recomputacao via SideOutputs; 24 tests novos, suite 155 passed (bebbb9d).
- Dirty lab `2026-05-24-cpf-templated-checked/` aberto (Pacote 7 aprovado pelo owner), sub-exps 01-04: M10 puro PIORA CPF (ratio 126% uniform); variante B (strip + check-elide + base-94) vence com -64% vs raw; H1 REFUTADA; RT FAIL em D-CPF-corrupt detectado — pre-tx "corrigia" check invalido silenciosamente (14eb62a).
- Dois feedbacks do owner viraram padrao (memorias user salvas): outputs .tcf visiveis pra auditoria em out_tcf/ (e562885) e decode + RT per-row OBRIGATORIO antes de reportar bytes — sub-exps 01/03/04 refeitos, 11 mismatches do corrupt expostos (b0fc7af).
- Sub-exps 05-10 na sequencia: fallback marker `_` (literal vs compressed) da RT 100% nos 9 datasets da progressao dirty, edge-allsame em 0.08% e edge-allcorrupt em 134% — pre-tx precisa de heuristica de aplicacao (989c3dc); NatureApplyStats com dimensoes ISO 25012 + mapeamento academico dos datasets (Beizer/Myers/DeMillo/Miller) valida heuristica apply>=0.5 AND consistency>=0.5 (e7095d6); H3 CONFIRMADA — TemplatedCheckedSpec parametrico serve CPF e CNPJ com zero codigo novo alem da spec, RT 100% em 18 datasets (f433917); IP TCU-Delta — variante C (padded 12-digit) chega a 1.71% em D-IP-subnet via seq-RLE, mas IP nao cabe na spec checked (sub-categoria nova) e ha' RT FAIL em mixed/hostile (2a9cf1a); padding-aware fallback resolve — RT 100% nas 27 medicoes, bytes extras aceitos como custo do lossless (18568c4); sub-exp 10 debug com dump de side outputs expoe bug de calibracao do gating min_len (ADR-0010) em variable-length — 200 IPs subnet: 68.17% com gating ativo vs 2.28% padded; implicacao registrada pro canonical (11ace10).

## Fontes
- git log 2026-05-24 (14 commits: ad5afeb..11ace10), bodies; checkpoint notas/checkpoints/2026-05-24-sessao-maxima-natures-multi-delta.md (era, 2026-05-26) so' como conferencia.
