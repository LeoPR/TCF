# 2026-07-06 — TCF.8H: header consolidado + Ciclo 1 de tipos + tipos-como-specs + spec_bin

> **ENTRADA RETROATIVA** (reconstruída 2026-07-09, T-CLEAN-3 T3-a) a partir dos commits do dia
> (git log) + labs dirty 2026-07-06-* + notas tcf8h-* / tipos-como-specs.md + EXP-015 report.
> Escrita na chave da ÉPOCA — sem conhecimento posterior.

## O dia

Sessão contínua no estudo TCF.8H (hierárquico, ramo do header-minimal / EXP-015), em 3 blocos.

- **omit-closes default** no codec EXP-015 (RT-exato, S6 154→153B); `analise_header.py` caracteriza
  SAVING(L)=digits(size)+depth; abre T-FMT-TCF8H-HEADER, T-FLOW-ENCODE-STRATEGIES-TELEMETRY e
  T-OPT-INFERENCE (649768d).
- **Checklist do cabeçalho em 5 camadas** (explícito/inferências/always-win/by-choice/cobertor-curto),
  142 elementos via workflow; corrige S4 67→66B e S6 154→153B stale no report (2f3906b).
- **Ciclo 1a — fidelidade de tipos** (00249ac): str(v)→JSON tipado não faz RT (30→"30", true→"True",
  null→"None"); conserto naive = tag 1 letra i/f/b/n colada no size, body JSON-canônico, RT 3/3.
  H-TYPE-01 no roadmap (confirmada-conceitual).
- **Ciclo 1b — tipo A/B/C + escala de formas** (330d148): owner pediu "medir os dois, decidir com
  número". A-explícita (lossless), B-dedução (LOSSY silencioso: "01310"→1310), C-híbrida (tag só na
  colisão). Decisão: **C=default**, A=fallback. Chave-ausente + null-em-array-misto = fronteira → 1c.
- **Ciclo 1c — fronteira do link posicional** (fd1857d): B1 chave-ausente / B2 null = def-level;
  B3 array-em-array = rep-level; B4 N:N = só normalização (não é gap de RT). Fix nullable/presença:
  máscara 3-estados `.`/`0`/`-` (definition level do Dremel em forma textual), RT-OK em B1+B2.
  **Fecha o Ciclo 1** (funcionalidade); B3/B4 deferidos ao welding.
- **Reframe do owner — tipos como SPECS** (3b7562a): primitivo = spec ultra-minimalista induzida
  (mesmo espectro das natures); toda spec se justifica por COMPRESSÃO ou ACELERAÇÃO; indução segura
  via round-trip SSE ('30'→int ok; '01310'→string). Correção empírica: bool em TEXTO ganha só ~6B
  flat; compressão por-valor (bitmap) é binária (V2-L). Nota tipos-como-specs.md criada.
- **Boolean nos datasets** (313318c): ZERO true/false em dado real (synthetic + hubs adult/tpch/
  receita/ibge/br); o que existe é enum-2/3 com superfície=DADO (Male/Female, matriz_filial=1|2).
  Primitivo útil = **ENUM/domínio-k**. 8 eixos ortogonais de spec (owner). adult.sex N=48842:
  raw 97KB → textual 49KB → bitmap 6KB.
- **Motor spec_bin** (9b9a730): enum-2 SEM catálogo (escape: os 2 mais comuns = domínio, guardados
  1× afixo-comprimidos); corpo bit-stream; motor testa RLE (textual/explicável) vs packed (binário
  N/8) e escolhe. Real espalhado: packed vence nas colunas medidas; overlay de exceções (canal
  esparso) RT-OK. Tensão de explicabilidade registrada: preferir RLE quando perto; packed é V2-L.

## Fontes

- git log 2026-07-06 (8 commits: 649768d, 2f3906b, 00249ac, 330d148, fd1857d, 3b7562a, 313318c,
  9b9a730) + labs dirty `2026-07-06-{2221,2238,2246,2310,2332,2354}-*` + notas
  tcf8h-header-checklist.md / tcf8h-proximas-ideias.md / tipos-como-specs.md + EXP-015 report.md +
  roadmap-hipoteses.md (H-TYPE-01) + tickets abertos no dia.
