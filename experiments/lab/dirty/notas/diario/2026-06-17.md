# 2026-06-17 — Filtros modulares F1/F1.5; F2 parado em (A); H-QUERY-04 design; cheap-wins Tier A

> **ENTRADA RETROATIVA** (reconstruída 2026-07-09, T-CLEAN-3 T3-a) a partir dos commits do dia
> (git log) + bloco SESSAO 2026-06-17 do STATUS.md + ADR-0027 e design notes da era.
> Escrita na chave da ÉPOCA — sem conhecimento posterior.

## O dia

- **F1 FEITO** (e1b031a): gadget `scripts/natures_compiler/` compila definicao TEXTUAL
  declarativa (.dsl flat, sem YAML) em spec executavel do core (TemplatedCheckedSpec/Padded),
  com round-trip lossless no compile-time e check_fn de biblioteca FECHADA nomeada (zero eval).
  Prova: spec compilado do DSL == canonico a mao (CPF/CNPJ/IP). 9 testes; suite 434.
  Achado honesto: CEP/MAC NAO cabem no TemplatedPaddedSpec (zeros a esquerda / hex) —
  spec novo ficaria pra fase futura com aprovacao + GATE.
- **F1.5 FEITO** (6eabaeb): registry por nome (get/register/names/load_dir), semeado com
  cpf/cnpj/ip; usuario resolve o nome e passa o objeto pro encode (API inalterada). +5 testes
  (14 no gadget; suite 439). Pesquisa CEP + outer-dict: o TCF JA trata CEP losslessly com
  zeros preservados (93B vs 119B raw) — nao precisa de nature nem codebook; outer-dict
  subsumido por V2-B+split no tabular; registrado H-CODEBOOK-01 (baixa prioridade).
- **F2 (H-NAT-MARK-01, nature-id no header #TCF.8)**: design fechado via workflow, 3 lentes
  (0c2bc4d); **decisao do owner = (A) nao implementar agora** (72b0a7f) — magic permanente nao
  se justifica so' por DX (gate >=15%/2-reais nao bate; registry gadget ja' cobre).
  ADR-0027 fica `proposed` (docs-only); index ADR completado (0025/0026/0027).
- **H-QUERY-04 design** (0fa6377): decode = DAG de 5 nos; corte minimo por query so' em
  @dict/raw (tcf entrelacado = fallback total); decode unificado = `execute()` no GADGET;
  indices escondidos: derivavel > sidecar > NUNCA in-blob por default. **Revisao do owner**
  (f1c5cd6): unificacao bottom-up (fazer cada op -> fatorar o comum), paralelismo por coluna,
  indice in-file como meio-termo, decisao de indice POR PERFIL de uso.
- **Cheap-wins Tier A** (de11219): `release.yml` (Trusted Publishing OIDC + gate byte-canonical
  antes de publicar), `docs/reference/encode-knobs.md` (Diataxis), higiene ci.yml; secao
  cheap-wins do ROADMAP reorganizada com criterio de done.
- `src/tcf` INTOCADO o dia inteiro.

## Fontes

- git log 2026-06-17 (7 commits: e1b031a..de11219, subjects + bodies)
- STATUS.md bloco "SESSAO 2026-06-17"; `f2-nature-mark-header-design.md`,
  `cep-outer-dict-codebook-pesquisa.md`, `hquery01-decode-dag-indices-design.md`, ADR-0027
