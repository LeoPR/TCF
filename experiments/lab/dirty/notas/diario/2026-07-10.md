# 2026-07-10 — pré-verificação de release 0.8.0 + planejamento do material comprobatório (T-QA-8)

## Contexto

Continuação da sessão pós-T-CLEAN-3 (fechado 07-09, `dab35cf`). Dois blocos hoje:
(1) push + pré-verificação técnica do release 0.8.0; (2) diretriz nova do owner —
planejar (SEM executar) o **material comprobatório** de tudo do `.8`.

## Bloco 1 — release 0.8.0 pré-checado (falta só o go)

- Push dos 5 commits T-CLEAN-3 (`b38d2c8..dab35cf`).
- Consistência de versão 0.8.0 nos 3 pontos (pyproject/`__init__`/pino) verificada; zero loose
  ends reais em src/tcf.
- Suíte completa: **530 passed** (gates D1-D9=1523B, D17a=300B, real-world=89616B intactos).
- `uv build` → sdist + wheel 0.8.0 (Cython compilou); **clean-room smoke** em venv isolado passou
  inteiro: import, RT single órfão, RT multi `#TCF.8M` hex, RT escaping (`a\:b\,c\=d`), LazyTCF.
- Registrado em T-DIST-RELEASE-0.8.0 (C3 pré-checado; commit `1687730`). PyPI segue 0.7.1 até o
  go explícito do owner.

## Bloco 2 — T-QA-8: plano do material comprobatório (dispositivo do dia)

Diretriz do owner: material comprobatório com todos os **dicts**, verificação de **paralelismo**,
**telemetria** (interna via SideOutputs + a "de graça" do Python/OS); escalonado controle-minúsculo
(single-col com/sem header, variações de reader, exemplo-propaganda do README) → sintéticos →
públicos; regra de anonimização de CPF (medir com DV-válido, publicar anonimizado); bugs achados =
só REGISTRAR; depois otimização extra e pip com documento bem feito.

Levantamento por workflow (10 agentes: 6 inventário read-only + 4 sweep adversarial com repros).
Resultado consolidado em **`tickets/T-QA-8-material-comprobatorio.md`** (P1, fases F0-F6).

| decisão/achado | detalhe |
|---|---|
| Dicts = 3 welded + 2 lab | V2-B `@` (cap 8192, sem teste de boundary/width≥2), dict implícito HCC (`^eid`), SPEC_REGISTRY natures; lab: cross-dict B1/B2 (closed), bN/spec_bin (gated) |
| Paralelismo JÁ EXISTE | `encode(parallel=)` ProcessPool welded (T-CODE-ENCODER-MANAGER); gaps: byte-identidade só em D17a, porção serial pós-pool não medida, decode serial, Cython sem nogil |
| Telemetria Windows | process_time tick 15.625ms → mediana n≥9 obrigatória; psutil NÃO é dep; workers inobserváveis via stdlib; obat_log/hcc_trace gerados incondicionalmente (custo sempre pago) |
| Regra CPF operacionalizada | DV-inválido cai em fallback literal `_<valor>` (apply_rate 0.0) → medição (DV-válido efêmero, seed) e publicação (anonimizado rotulado) são artefatos DISTINTOS; publicar `.tcf` de CPF válido = publicar o CPF (self-describing decoda sem spec) |
| 10 bugs registrados (§3) | destaque: nome de coluna `''` → perda silenciosa de coluna (`'' in "!@%"` é True, multi/core.py:72); view crasha onde decode funciona (paridade); 0-rows→1-row; `#TCF.9M` KeyError cru; body truncado decoda calado; SideOutputs multi reporta candidato, não emitido |
| 5 doc-drifts (§3) | README embarcado na wheel ainda é 0.7 (exemplo 244B→242B medido; nega o self-describing que a 0.8 entrega); docstrings src/tcf; spec exemplo contradiz regra; pyproject sem urls/classifiers |
| Sequenciamento | publicação 0.8.0 agora passa POR DENTRO do T-QA-8 (F0 fixes sob aprovação → F1 harness → F2-F4 medição → F5 otimização gated → F6 docs+pip) |

**Nenhum fix aplicado** (regra do owner: registrar apenas). Higiene mínima com rastreabilidade:
row do T-FMT-NAME-ESCAPING re-rotulada OPEN-PARCIAL (interim welded em `58f7dee`, faltava refletir).

## Bloco 3 — F0 lote 1 executado (BUG-01+02+07, aprovação + design do owner)

Owner avaliou os 10 bugs e decidiu: focar 01+02+07, com design definido — `''` = coluna SEM nome
(entrada TRANSFORMADA, warning, "assim evitamos scape"; escape-vazio no decode = ERRO, marcado pra
um futuro reparador → ticket novo); view com o MÍNIMO de verificação (check implícito); `body_bytes`
MANTÉM semântica de candidato (artefato de custo compute/memória) e o emitido conta-se NO processo
("contar no final é serializado").

Execução red→green (16 repros pinados em `tests/test_f0_boundary_fixes.py` ANTES dos fixes):

| fix | como |
|---|---|
| BUG-01 | fronteira: `''`→anônima + UserWarning + ValueError em colisão posicional (exceto drop_names); decode fail-loud marca corrupção (`<size>=`, dangling ímpar, hex inválido) |
| BUG-02 | `_parse_meta` = FONTE ÚNICA do parse do meta (core+view) — paridade por construção, zero verificação extra; idiom `[:1] in "!@%"` eliminado; `is_v8` morto removido |
| BUG-07 | `emitted_bytes`/`emitted_mode` per-col + `multi_info['col_modes']`, capturados no ponto do min() (contagem já existia pro size do header — zero passada extra) |

**Verificação adversarial** (workflow 3 agentes): byte-neutralidade old(HEAD)-vs-new **122/122**
(encode+decode+side_outputs+parallel); refutador (320 checks) achou 1 ALTA real — gramática ambígua
do último token (`<size>` bare com `min_header=False`+anônima parseava como NOME; pré-F0) — fechada
no EMIT (última anônima SEMPRE sem size) + falso-positivo do guard com drop_names fechado; contrato
view/SideOutputs sem regressão (paridade até em natures e meta-vazio). Achados novos REGISTRADOS
(não fixados): BUG-11 (leniência do unescape: fusão por `\` inserido + aliasing de escape
não-separador) + confirmação com repro do BUG-05. Suíte final: **546 passed**, pins intactos.

Tickets: [T-QA-8 §3](../../../../../tickets/T-QA-8-material-comprobatorio.md) atualizado (01/02/07
fixados, F0-1 lote 1, F0-2 done); novo
[T-TOOL-TCF-FIX-CORRUPTION](../../../../../tickets/T-TOOL-TCF-FIX-CORRUPTION.md) (ideia do owner:
reparador de corrupção; o decode agora marca os ganchos).

## Bloco 4 — F0 lote 2 executado (BUG-03+04+05+06, decisões do owner)

Owner avaliou os 4 em detalhe e definiu: 03 fail-loud AGORA + registro do trilho de armazenamento
(registro-'0' declara schema pra append→parquet/`tcfx`); 04 = subversões são controle de dev, visão
`#TCF1`(M) fecha tudo no 1.0, compat real só depois dele; 05 = os 3 cheques agora + registro do
profundo de streaming (receptor saber QUANTO esperar; fim-antes-do-aviso; timeout→truncamento);
06 = fundir check+stringify. Execução red→green (20 repros novos; xfail histórico de `encode([])`
virou contrato `raises`).

| fix | como |
|---|---|
| BUG-03 | ValueError em 0 linhas nos 2 ramos (colide com 1-linha-vazia por construção) |
| BUG-04 | versão deduzida do run COMPLETO de dígitos do magic (`.85` não vira mais disc `'5'`); `.6/.7` mantêm git-hint |
| BUG-05 | decode: size vs disponível + fecho do blob + cross-check n_rows (invariante nunca gravado, deduzido de graça); view: size+fecho (lazy sem n_rows, deliberado) |
| BUG-06 | validação `\n`/`\r` fundida na passada do `_to_str` (valida pós-transformação; 1 passada em vez de 2) |

**Verificação adversarial** (workflow 3 agentes): byte-neutralidade **189/189**; refutação falhou em
46 combos íntegros; falso-positivo do órfão **fecha por construção** (HCC escapa dígito pós-literal:
`['#TCF.9M x']` → body `#TCF.\9M x`, RT ok). **Eficácia medida** (1474 cortes + 503 flips):
truncamento calado-errado 29.2%→3.0% (fora da zona órfã: 0.6%; blobs 100%-sized: 0 buracos);
byte-flip loud 42.3%→56.9%; excedente sized 0/5→5/5. Registrados sem fixar: **BUG-12** (alta,
PRÉ-existente: hang >1000s no decode HCC sob size-flip — toca o CORE, lote próprio) e **BUG-13**
(residuais: nome-flip sem checksum, nature-id desconhecido segue com warning, view EOF-truncado
materializa calado, IndexError críptico em dict+byte extra). Suíte: **566 passed**, pins intactos.

Registros profundos → **O-FMT-20..23** (Categoria 7 de `futuras-otimizacoes-formato.md`):
schema-declare/parquet/tcfx · auto-stamp em colisão de magic · rumo ao `#TCF1` · completude de
transmissão/streaming.

## Bloco 5 — F0 lote 3 executado (BUG-08+09+10+11b, decisões do owner)

Owner detalhou os restantes e definiu a filosofia da rodada: **fronteiras primeiro, já pra ter os
ISOLAMENTOS** — "o código tendo tratamento pode identificar eles e a gente pode mudar
comportamento" (o núcleo trabalha strings, mas há tipos anterior/próximo, diffs e as questões dos
specs — inspeção profunda fica pra ANTES do 1.0, em ticket). Execução red→green (16 repros novos):

| fix | como |
|---|---|
| BUG-08 (fold) | `#TCF.8M\n` (meta vazio SEM body, não-emitível) → ValueError decode+view; meta vazio COM body segue legítimo |
| BUG-09 | str/bytes como coluna → TypeError que ensina (sem auto-embrulho: declarar > deduzir) |
| BUG-10 (7 subs) | list converte não-str (=dict, check fundido; `_reject_linebreaks` removido); layers/parallel/decode validados na porta; **parallel=1 → serial deduzido** (sem spawn); name= sem nature e natures cruzados → erro; stamp+dict segue ignorado (M já é stamp) |
| BUG-11b | whitelist `_ESC_OK=",=:\\!@%"` — escape não-estrutural → "meta corrompido" (dangling no mesmo scan) |

**Verificação adversarial** (2 agentes): refutação FALHOU (~310 checks; zero blob legítimo
rejeitado; whitelist ≡ exatamente o que `_esc_name` emite); byte-neutralidade **103/103** +
`parallel=1 ≡ serial` byte-a-byte + decode cross-check. Notas: `parallel=-1`/`2.0` e `name=`
ignorado eram tolerâncias fora-de-contrato — agora erro (intencional). Suíte: **582 passed**.

Tickets novos (pedido do owner): [T-API-BOUNDARY-CONTRACTS](../../../../../tickets/T-API-BOUNDARY-CONTRACTS.md)
(revisão pré-1.0, tabela caso-a-caso) + [T-FMT-META-STRICT](../../../../../tickets/T-FMT-META-STRICT.md)
(dedução-do-cânone vs checksum; vincula BUG-11a residual, 13b/d/e, T-TOOL).

## Bloco 6 — F0 lote 4 ("vamos fechar os A") + flag de paralelismo + F0-3

Owner pediu a lista do resolvível-agora e mandou fechar: **lote 4 = BUG-13 b/d/e + DOC-02**
(red→green, 8 repros novos; suíte **590 passed**; decode-only + docstrings — encode intocado por
construção). Também: registrado **T-CODE-PARALLEL-BUDGET** (flag de paralelismo/uso de CPU —
env `TCF_MAX_WORKERS` como teto do host; design pós-F3) e **F0-3 fechado stdlib-only**
(psutil só como `[bench]` opcional se o F3 provar necessidade).

| item | o quê |
|---|---|
| BUG-13b | nature-id desconhecido → ValueError (decode multi+single E view) — **revoga o forward-compat de 2026-06-24** (2 testes re-pinados com rastreabilidade; pre-1.0 sem compat, ADR-0024) |
| BUG-13d | cross-check incremental na view: `_col()` compara len com coluna já materializada (custo zero, laziness intacta) — view não devolve mais dado errado calado em blob truncado |
| BUG-13e | invariantes internas: V2-B ntable bound + stream%width + **índice∈tabela** (byte de editor virava índice negativo e wrapava a tabela EM SILÊNCIO — pior que o IndexError registrado); split ntmpl bound; `_dict_parts` da view em paridade |
| DOC-02 | docstrings de src/tcf sem mentira: `__init__` (formato/D17a 300B), encoder (Args/Raises reais: parallel novo, natures self-describing, name/stamp/drop_names), multi/core (contratos pós-M2/F0), dict_v2b, view (só 8M; parser único), natures (exemplo decode sem spec) |

**Placar F0: 12 de 13 bugs resolvidos em 4 lotes** (60 repros pinados). Resta só BUG-12 (hang
HCC — lote próprio, CORE) + residuais-de-checksum 13a/c (trilho tcfx). O levantamento de tickets
pro fechamento do .8 foi MATERIALIZADO em
[T-REL-08-CLOSEOUT](../../../../../tickets/T-REL-08-CLOSEOUT.md) (lembrete do owner: "mesmo a
ordem de execução gera tickets — rastreabilidade e memória, não pode ficar tudo só na conversa"):
Passo 1 higiene de status → Passo 2 caminho crítico (F1→F6→C3) → Passo 3 fronteira .8.1/.9/pré-1.0.

## Aberto pra próxima sessão

- Owner: higiene de status dos tickets welded (HEX, DESCAPAR-V2B, TRANSMISSION-GROUPING,
  NAME-ESCAPING, TCF8H) + largada do **F1** (runner de telemetria) — caminho crítico do .8.
- BUG-12 quando o owner abrir o lote (CORE, gate completo).
- Push quando o owner pedir (commits locais: b9b290c, a2e84b0, 31dd799, a0f30ae, 80b6ad7 + lote 4).
