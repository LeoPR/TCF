# Diário 2026-07-15 — hierarquia: amostragem honesta, auditoria adversarial, fix de escaping

Continuação do weld do `#TCF.8H` (2026-07-14). Sessão focada em **honestidade metodológica** e no
que ela desenterrou.

## O que foi feito (na ordem)

1. **L3 de-firmado** — a multiplicidade explícita×deduzida virou HIPÓTESE (bloco de otimizações;
   eixos latência/memória/velocidade/compressão), não veredito. Owner: multiplicidade é 1 exemplo,
   não o fenômeno. `H-L3-OPT-BLOCK` aberta, deixada pro fim.
2. **Fixar o óbvio + testar em massa** — fuzz sintético 8000/8000 (lab 2120) + property-test seedado
   em `tests/`. Depois **dado real**: TPC-H aninhado via FK (lab 2231), RT byte-exato 1500/15000 docs.
3. **Formalização (G1)** — ADR-0033 (weld welded), `H` marcado welded no char-registry, W0-W4 fechados.
   `src/tcf` **aprovado arquivo-a-arquivo** (condicionado à estabilidade, demonstrada pelo massa) → W5.
4. **Anotações do owner** (hipótese, não implementar): `H-DISC-ACCEL-01` (discriminador = dica pra
   accel compilado + mimemagic, modularidade não `if` rígido) + "não soldar demais" (L3 decouple = `.9`).
5. **Shaper** — confirmado que NÃO produz hierarquia nativa (`JOIN_LEVELS` só normalized|flat).
   Ticket `T-SHAPER-NESTED-OUTPUT` (P3, até 1.0) registra o `join_level="nested"` (inverso do flat).
6. **Estudo de amostragem HONESTA** (lab 2336) — resposta à crítica do owner ("representação
   selecionada ≠ amostra do todo; podemos pegar por sorte o que funciona"). Shaper `fk_preserving`
   (Neyman + integridade ref.): 18 estratos honestos RT 18/18 + frontier (N:N) + 2ª fonte + value-scan.
7. **Auditoria adversarial** (workflow `wf_4960112e-d45`, 4 lentes + síntese) do próprio estudo. Achou:
   - **BUGS R0**: nomes de chave com `,`/`{` → corrupção SILENCIOSA; `[`/`]`/`}` → HANG; `:`/`#` →
     fail-loud tardio. Causa: `_build_meta` emitia nomes crus; o `.8M` já escapa, o `.8H` não portou.
   - **Super-afirmações**: "ESTRUTURAL" (era robustez-a-valores numa topologia); br-identidades
     rotulado N:N (é 1:N puro, fan-out 1.03); "N:N fail-loud" (é INEXPRESSÁVEL no contrato, não guarda).
   - Verifiquei tudo com probes pinados antes de aceitar (a auditoria errou alguns medos: espaço/`\`/
     `\t`/`\x00`/vazios eram robustos).
8. **Fix welded** (`40a7e10`, aprovado pelo owner) — escaping de nomes portado do `.8M`
   (`_esc_name`/`_unesc_name` estrito + `nm()` escape-aware + `_rstrip_closes` preservando closers
   escapados). Suíte **666 passed**, pins flat verdes, probes 13/13. Re-rotulagem honesta do result.
9. **Probe de dado real não-sintético** (ROI-7) — receita-cnpj matriz→filiais: 200000 estabelecimentos,
   fan-out **max 396** (cauda pesada real), RT byte-exato na pop. inteira + amostra estratificada com
   null coerido. Null cru = coerção de tipo silenciosa (H-TYPE-01) → reforça contrato null pro fim.

## Estado

- Weld `#TCF.8H` verde e **mais robusto** que ontem (escaping de nome fecha uma classe de corrupção).
- Confiança de contenção 1:N: **Média-Alta** (agora com fonte real não-sintética de cauda pesada).
- Gaps HONESTOS registrados (result lab 2336 + ticket): cobertura topológica é sintética (fuzz);
  contrato tipo/null/ragged pro fim; N:N/multi-pai = super-hierarquia; online-retail (build) pendente.

## Aberto pra próxima sessão

- **Owner controla push** (vários commits à frente de origin/main, não pushados).
- **Contrato de borda do `.8H`**: além de `\n`-em-valor (herança do core, T-API-BOUNDARY-CONTRACTS),
  agora há a gramática-de-NOME (escaping welded) a **congelar** antes do freeze pré-1.0.
- Próximo incremento FUNCIONAL da hierarquia (owner escolhe): ragged (def-level) OU abrir tipos/null.
- Probe online-retail (InvoiceNo→itens) precisa build de hub (`csv_to_sqlite`) — não zero-custo.
- `T-SHAPER-NESTED-OUTPUT` quando for mexer no ferramental (até 1.0).
