# 2026-06-21 — Guia de transmissão, checkpoint Strata, faxina, A4 (view no core)

> **ENTRADA RETROATIVA** (reconstruída 2026-07-09, T-CLEAN-3 T3-a) a partir dos commits do dia
> (git log) + checkpoint `2026-06-21-avaliacao-documental-strata.md` + STATUS.md blocos SESSAO
> 2026-06-21/21-b. Escrita na chave da ÉPOCA — sem conhecimento posterior.

## O dia

- **Guia "onde o TCF importa em API"** (e788f22, `transmissao-api-onde-tcf-importa.md`): pesquisa em
  3 lentes web; achado honesto — a prática é JSON pequeno+gzip/brotli (TCF NÃO ajuda na maioria);
  nicho ~5-15% (batch/export tabular grande+repetitivo como pré-processo do brotli; lazy). TESTE
  DECISIVO PENDENTE: TCF+brotli vs NDJSON+brotli (só comparamos com CSV+brotli). Check do owner no
  header byte-size: econômico em tabela real (0,01-0,03% do blob); só pesa em payload minúsculo.
- **Checkpoint Strata** (d67f7c9): pausa + revisão; avaliação L0 honesta, média ~3,7/4 — separação/
  rastreabilidade/honestidade fortes; gaps de ACHABILIDADE achados e FECHADOS (MAP não listava 5 docs
  novos; STATUS ganhou bloco SESSAO 06-21). Vigiar §3-bis (dispositivo/probatório nas notas), §5
  (números copiados — manter o lab como fonte), §9 (proliferação de docs de compressão). Árvore
  limpa, 0 unpushed; `src/tcf` intocado na sessão inteira.
- **Faxina dirty/docs pré-0.8** (4401046, fases 1-5): `_wf_*.js` + 116 `out_files/` destrackeados;
  17 labs → `old/welded|refuted` via git mv (0 links quebrados); banners HISTORICO/SUPERSEDED em 5
  notas; shebang #TCF.6→#TCF.7 em how-to/tutorial; diário README 3→9 entradas; MAP.md com árvore
  `src/tcf` completa.
- **A4 FEITO — lazy view promovido pro core** (335b63d; owner aprovou o toque em `src/tcf`):
  `scripts/tcf_lazy/lazy.py` → **`src/tcf/view.py`** via git mv (camada read-only; `from tcf import
  view`); shim de compat em `scripts/tcf_lazy/`. Aditivo: NÃO muda encode/decode/formato; zero
  regressão byte-canonical (D1-D9=1523B, D17a=303B, real-world=89616B); 380 passed. Versão segue
  0.7.1 (bump 0.8.0 é no release, workstream C). 4 tickets 0.8 criados (A4 closed, A5, B1, C);
  B1 segurado por decisão do owner (foco no workstream A).

## Fontes

- git log 2026-06-21 (4 commits: e788f22, d67f7c9, 4401046, 335b63d) + checkpoint Strata do dia +
  STATUS.md (SESSAO 2026-06-21 e 2026-06-21-b) + `v08-plano-etapas.md`.
