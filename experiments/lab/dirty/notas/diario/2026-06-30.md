# 2026-06-30 — B2: design revisado (A-D) + prototype `&<G>` com RT lossless

> **ENTRADA RETROATIVA** (reconstruída 2026-07-09, T-CLEAN-3 T3-a) a partir dos commits do dia
> (git log) + `design-b2.md`/`design-b2-revisao.md` e `2026-06-27-gdict-b2-prototype/result.md`
> (artefatos datados 2026-06-27, commitados neste dia). Escrita na chave da ÉPOCA — sem
> conhecimento posterior.

## O dia
- **B2 revisado — correções A-D aplicadas** (fe8ceff; owner: seguir A-D; `src/tcf` intocado, weld
  é B3). Reescreve o design pós-revisão adversarial (24 achados, 0 blocker), já estreitado a
  same-domain (re-segmentação) e com respaldo da bibliografia:
  (A) wire-format: group-dict NÃO é linha de header (#TCF.8M não tem header multi-linha) →
  **prelúdio length-prefixed no início do corpo** + coluna grupada = slot novo stream-only, modo
  `&<G>` (`&` reservado junto de `@~%`); (B) dobradiça: `bytes(tab_G)` MEDIDO encodando a união
  (dedup não-linear sob HCC) + custo de header no modelo — **o termo de custo é o guard, não o
  Jaccard**; ordem-de-união = invariante a pinar; (C) lazy honesto: "decodáveis independentemente
  após prelúdio" (não há decode paralelo); cache keyed-por-grupo em view.py fica pro B4;
  (D) gate N≥5 (SNAP+OpenFlights=3, faltam ≥2 → liga T-DATA-1), overfit declarado, brotli =
  controle (reportar sempre, nunca gate).
- **Prototype read-only do B2** (f9c957e, lab `2026-06-27-gdict-b2-prototype/`, dirty-lab completo
  com artifacts: input, decisão de particionamento, OBAT log + HCC trace da união, blobs
  byte-a-byte, RT). Vai além do B1 (que só modelava bytes): constrói o wire-format corrigido e
  **prova RT lossless em todos os casos**. Achados: (1) formato `&<G>` funciona — correções A se
  sustentam na prática; (2) share valida o B1 exato: SNAP −19.3%, OpenFlights −4.6/−6.6%, escala
  com ≥3 col (decodes 3→1); (3) share **sobrevive gzip** (SNAP −21%) — redundância genuína, ao
  contrário do EI; (4) guard funciona ao escalar: borda união-cruza-bucket N=8000 → grp=0 (custo
  de largura domina; Jaccard alto NÃO poola).
- **Achado separado (não-B2)**: dict per-col SEM o cap 1024 vence OBAT/HCC em −29..−36% pra
  categórico high-card alta-repetição (N/K≈5.5) e sobrevive gzip — o cap do V2-B deixa 30%+ na
  mesa. Ortogonal ao cross-dict → **H-DICT-HIGHCARD**, ticket próprio, gate N≥5 antes de weld.
- **Pendência B3**: gate N≥5 do cross-dict (3 fontes medidas, faltam ≥2 → T-DATA-1). Registrado
  na fila + plano.

## Fontes
- git log 2026-06-30 (2 commits: fe8ceff, f9c957e) +
  `experiments/lab/dirty/2026-06-21-gdict-caracterizacao/design-b2.md` e `design-b2-revisao.md`,
  `experiments/lab/dirty/2026-06-27-gdict-b2-prototype/result.md` (versões da era via git show).
