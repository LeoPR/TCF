# 2026-06-25 — Discriminador #TCF.8, plano consolidado, lazy le #TCF.8, H-NAT-SPEC/CPF, escape-invertido

> **ENTRADA RETROATIVA** (reconstruída 2026-07-09, T-CLEAN-3 T3-a) a partir dos commits do dia
> (git log) + `tcf8-estrutura-plano.md`, `hipotese-filtro-natureza-especulativo.md`, lab
> `2026-06-25-cpf-duas-vias/`, ADR-0029. Escrita na chave da ÉPOCA — sem conhecimento posterior.

## O dia

Sessao de madrugada (00:04-03:39, 17 commits), continuacao direta do #TCF.8 de ontem:

- **Discriminador #TCF.8 de 1 char** (realiza ADR-0029): o char logo apos `#TCF.8` decide —
  `M` multi (sem espaco, meta INLINE no shebang, ~2B a menos), espaco = single+spec, `\n` =
  version-stamp opt-in (magic-number pra file/libmagic). Byte-neutro (tudo opt-in; single puro
  segue orfao), +6 testes, suite 405 (a192466, f1115b0).
- **Colunas anonimas/posicionais** via `drop_names` (nome = ordem, como SQL 1,2,3 / CSV sem
  header): omite `=nome` no meta, forca #TCF.8M; parse position-aware desambigua; bug #TCF.6
  min_header pego no GATE. Opt-in, +6 testes, suite 411 (70eb7f7, e58049c).
- **Plano consolidado** `tcf8-estrutura-plano.md` (fonte unica): #TCF.8 = FAMILIA
  self-describing, features = desvios opt-in marcaveis; sequencia 1) lazy-view #TCF.8 2)
  congelar single-col @1.0 3) release 0.8.0 4) cross-dict 5) tcfx index (0.9). Cross-dict
  reconciliado como feature DENTRO do #TCF.8 (decisao do owner) (6ad19a4). Hipotese regex como
  otimizacao: avaliada, prioridade baixa — core nao e' regex-shaped (5ff295f).
- **Lazy-view le #TCF.8** (passo 1 do plano): meta inline, nature revertida LAZY (so' a coluna
  consultada decoda), colunas anonimas. Read-only/aditivo, +3 testes, suite 414 (cb9b979,
  3e92630).
- **H-NAT-SPEC** (filtro de natureza especulativo, proposta do owner) levantada e refinada em 3
  rodadas: eixo real = normalizar-e-comprimir vs codificar-denso (base94); fluxo de duas vias
  e' o ponto (f92d6fc, 9a76f6a, 66a647e). **Piloto CPF**: aleatorio -> base94 vence
  (13682 vs 23793); cadenciado -> 9dig-OBAT ~150x; consumidor do modo-1 = ID formatado
  sequencial (NF-e/fatura), nao CPF (b7657a0). En masse N=20k: base94 29.9% sob brotli;
  no-obat NAO byte-neutro (afixo espurio em 7.36% dos base94) (70428e6). Inspecao (3 casos):
  seq-RLE espurio no aleatorio; regua-brotli — TCF-nativo -> base94 (c3a0e2e). Status
  caso1/caso2 registrado: DV-drop e paralelismo FEITOS; alfabeto livre-de-conflito e
  producer-consumer NAO (este = V2-J streaming) (0260883).
- **Escape-invertido (EI) VIAVEL** — corrige rejeicao anterior: EI != escape-deduction
  (Pacote 2, fechada); flip GLOBAL por byte-count, nao depende de node_count. Medido: caso1-raw
  18.8%, suite sintetica 9.2% bruto; gate = real-world digit-heavy (48517e4). Reframe: global =
  prova (piso); alvo = polaridade inteligente preditiva/serializada (teto) (cd51cae).

## Fontes
- git log 2026-06-25 (17 commits: a192466..cd51cae) + tcf8-estrutura-plano.md, ADR-0029,
  hipotese-filtro-natureza-especulativo.md, lab `2026-06-25-cpf-duas-vias/`.
