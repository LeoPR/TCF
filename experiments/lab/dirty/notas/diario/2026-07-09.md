# 2026-07-09 — discriminador `H` (ADR-0031); hex-default do header; survey de bases/radix

> Consolidação datada [dispositivo]. Continuação da sessão bN/specs (ver [2026-07-08](2026-07-08.md)).

## 1. Discriminador `H` formalizado (ADR-0031)

Revisão do ticket **T-FMT-TCF8H-HEADER** + material relacionado. O único bloqueio dispositivo era o `H`
não-registrado. **Decisão do owner**: `H` = discriminador de hierarquia = especialização de `M`
(espaço=single · `M`=multi plano · `H`=`M`+hierarquia). Além de formalizar, dá dispatch **O(1)** no decode.
`H` é **sem-espaço** (`#TCF.8H<meta>`, herda de `M`) — o protótipo EXP-015 usou espaço e alinha no weld.
**ADR-0031** (accepted): reserva char + semântica; codec segue research-track, welding = ticket próprio
gated; `src/tcf` intocado. Discriminador do `.8` fechado em 5 valores. Ticket → `decided-weld-gated`.

## 2. hex-default do header (T-FMT-HEADER-BASE-HEX)

Levantamento do T-OPT-INFERENCE → **decisão do owner**: **hex é a base default IMPLÍCITA** dos byte-sizes do
header (armazenado sempre hex); **decimal NÃO é formato alternativo** — é só **comando** de apresentação
(inspeção/legibilidade/declarar-em-IO/debug). Como hex é ganho puro, não há razão pra decimal armazenado.
Ticket **super-específico** T-FMT-HEADER-BASE-HEX criado e concluído (`decided-weld-gated`), desmembrado do
T-OPT-INFERENCE Item 1.

**Princípio de compat (owner)**: retrocompat pré-1.0 é só comparativo evolutivo; **no 1.0 o passado morre no
git, sem `if .7`/`if .6`**. Logo hex-default é A base rumo ao 1.0, não um opt-in pra preservar #TCF.7 decimal.

## 3. Survey de bases/radix em cada parte do TCF (workflow, verificado)

Owner pediu revisar os usos de base em cada parte, fechando aos poucos. Survey (5 leitores + crítico,
file:line) → registro [`bases-radix-usos-tcf.md`](../bases-radix-usos-tcf.md). **Regra achada**: hex só
serve onde o número é **delimitado e não encosta em literal**. Os 4 baldes:
- **A (fechar agora)**: header sizes → hex. Toca `core.py:230,234,349,357` **+ `view.py:99,104`** (o lazy
  re-parseia — o survey pegou; naive-switch quebraria o `view()`). Move o pin **D17a** (não o real-world).
- **B (já ótimos)**: `@dict` base-94, bN bits, nature base-80 — hex seria PIOR.
- **C (LOCKED decimal)**: refs `^N`/ref-stream/seq-RLE — o digit-escape `\<digits>` colide com `a-f`; hex
  quebraria round-trip. **Decimal obrigatório no corpo.**
- **D (gradual/maybe)**: prefixos `<ntable>`/split, RLE counts — marginais, estudar depois.

Isto valida o escopo estreito do ticket: hex pertence ao header e a NENHUM ref do corpo.

## Propagação

ADR-0031 + índice + ADR-0029; char-registry; T-FMT-TCF8H-HEADER; T-FMT-HEADER-BASE-HEX (novo); registro de
bases (novo); cross-links (T-OPT-INFERENCE spin-off, checklist C4, O-FMT-18, omit-contract, tickets/README,
MAP); memória de versionamento (princípio compat). `src/tcf` NÃO tocado.

## 4. Weld do hex-header (FEITO — go do owner)

`multi/core.py` (emit `format(n,'x')` se magic≠#TCF.6; parse `_szbase=16 if is_v7/is_v8 else 10`) +
`view.py` (mesmo re-parse lazy, lockstep). **#TCF.6 legado fica DECIMAL** (322B inalterado — reprodução v1).
Re-pin **D17a 303→302B** (+ 7 cópias do pino). D1-D9/real-world (single-col) intactos. **528 passed.**
Verificado: #TCF.6=322 dec · #TCF.7=302 hex (`3d=timestamp,1c=id,90=email`) · RT · view lazy.
Ticket T-FMT-HEADER-BASE-HEX → **welded**. **Pendência de versão** (owner): refinou #TCF.7 in-place.
**CORREÇÃO (review pré-bump)**: blobs 0.7.1 decimais NÃO "falham" — **decodam silenciosamente ERRADO**
(decimal ⊂ hex sintaticamente; verificado). Resolução recomendada: hex = exclusivo da família .8, #TCF.7
volta a decimal legado-de-leitura — absorver no ADR do .8-default.

## 5. #TCF.8 vira DEFAULT — ADR-0032 + milestones M1-M4 (owner: fechar o .8)

Revisão crítica pré-bump (workflow 8 agentes) travou 8 decisões do owner → **ADR-0032** (accepted, +
ADR-0028 aceito). Execução em milestones:
- **M1** (flip+corte): `#TCF.8M` = default multi-col; legado `#TCF.6/.7` CORTADO de src/tcf (decode
  fail-loud); hex exclusivo do .8; single-col órfão intocado (D1-D9/RW). D17a re-medido **302→300B**
  (header inline −2B). Fail-loud no discriminador desconhecido (fecha a corrupção silenciosa do `#TCF.8H`).
  525→530 tests.
- **M2** (escaping): nomes com separador (`,`/`=`/`:`/`\`/prefixo `!@%`) via **backslash** (só a barra;
  quoting-implícito adiado). RT provado; único proibido `\n`. T-FMT-NAME-ESCAPING interim welded.
- **M4** (docs): spec `TCF-format.{pt-BR,en}` reescrita (#TCF.8 default + discriminador 5-valores c/ `H` +
  hex + escaping); CLAUDE/vocabulary/STATUS/ROADMAP/MAP/tcf8-estrutura-plano com bridge 2026-07-09
  (Strata: pont-não-reescreve datado). D17a=300B propagado.
- **Falta M5**: bump `0.8.0` + CHANGELOG (sem publicar; lazy absorvido, ADR-0028).
- **Nomenclatura de versão**: a pendência "hex in-place vs bump" foi RESOLVIDA pelo .8-default — hex é da
  família .8, não refino do .7 (o .7 saiu do código).

## 6. M4 tail fechado (A+B+C) — sem pendências (owner: fechar tudo, com rastreabilidade)

Bridges datadas que PRESERVAM o texto antigo (recuperável se arrependermos) + cross-link ADR-0032:
- **A (notas canônicas)**: bridge 2026-07-09 em `specs-capacity-map.md` (SSE-nature/byte-neutro/303B) +
  `tcf8-vista-o-que-falta.md` (opt-in/cross-dict) + `tcf8-estrutura-plano.md` (já no M4).
- **B (tickets)**: **T-DIST-RELEASE-0.8.0 RE-ESCOPADO** = agora é o release 0.8.0 de verdade (status
  blocked→open, blocked-by vazio, C1 bump 0.7.2→0.8.0, critério "sem #TCF.8"→"#TCF.8 default"); é o setup
  do M5. T-FMT-OMIT (tensão-H resolvida, ADR-0031) + T-OPT-INFERENCE (opt-in→default) + tickets/README (título).
- **C (cosmético)**: ROADMAP (view lê #TCF.8) + exemplo `#TCF.8...C` nas 2 specs.

## 7. M5 — bump 0.8.0 (FEITO, sem publicar)

`pyproject.toml` + `src/tcf/__init__.__version__` `0.7.1→0.8.0` + pino `test_version_pre_1_0` +
`CHANGELOG.md` (entrada 0.8.0: #TCF.8 default, hex, escaping, legado cortado, lazy absorvido).
`tcf.__version__ == "0.8.0"`; suíte 530 verde. T-DIST-RELEASE C1/C2/suíte marcados; **C3 (tag+publish)
fica pro go do owner** — PyPI segura em 0.7.1. **Fecha os 5 milestones do `.8`-default (ADR-0032).**

## Aberto
- Deferidos (fora do .8): weld codec TCF.8H (lab); baldes B/C/D do registro de bases; framework specs
  induzidas (T-OPT-INFERENCE); dict-interno B (H-TYPE-07).
