# 2026-07-11 — T-REL-08 Passo 1 (higiene) + Passo 2a (runner F1 do material)

> **Datação**: sessão CONTÍNUA iniciada em 2026-07-10 que cruzou a meia-noite — os blocos 5-7 do
> [diário de 07-10](2026-07-10.md) (lotes 3-4 do F0, flag de paralelismo, CLOSEOUT) e os commits
> `a0f30ae`/`bf84fca`/`50901c2` pertencem ao MESMO arco; este arquivo cobre o trecho do dia 11
> (F0-3 portável + Passo 1 + Passo 2a). Transparente, como no precedente de 05-27.

## F0-3 re-especificado PORTÁVEL (diretriz owner)

Telemetria = CONCEITOS independentes de OS/hardware/linguagem + SONDAS isoladas por plataforma
com fallback gracioso (detalhe no bloco 7 de 07-10 e no T-QA-8 §2.4/F0-3).

## Passo 1 do CLOSEOUT (commit `50901c2`)

5 rotulações rastreáveis (HEX, DESCAPAR-V2B, TRANSMISSION-GROUPING, NAME-ESCAPING, TCF8H-HEADER)
+ ticket-filho **T-FMT-QUOTING-STUDY** (.9, preferência do owner) + 2 linhas .9 no ROADMAP Tier 1.

## Passo 2a — runner F1 do material comprobatório (T-QA-8 F1 COMPLETO)

- **`scripts/bench_evidencia_probes.py`** — a camada F0-3: conceitos (`wall_time_ns`,
  `cpu_time_ns`, `measure_repeat` warmup+n+mediana/p95, `peak_heap_bytes`, `peak_rss_bytes`,
  `env_fingerprint`) portáveis 1:1 pra Rust; sondas confinadas (`getrusage-posix` com normalização
  de unidade POR OS e Solaris declarando-se indisponível; `k32-getprocessmemoryinfo` no win32 —
  o alias psapi não resolvia via ctypes, achado do smoke); fallback = campo AUSENTE, nunca crash.
- **`scripts/bench_evidencia.py`** — run_case (RT OBRIGATÓRIO antes de qualquer número;
  determinismo; bytes medidos do blob; timing encode/decode separados; memória em runs separadas;
  SideOutputs serializado com emitted/col_modes do BUG-07) + `--validate-pins` (F1-4) + CLI +
  JSONL versionado em `experiments/results/evidencia-0.8/` (exceção no .gitignore + README do
  schema `evidencia-0.8/v1`).
- **Régua batida exata**: D1-D9=1523B · D17a=300B · real-world=89616B (e virou teste da suíte).
- **Verificação adversarial** (2 agentes): isolamento OS-specific confinado às sondas (grep limpo);
  fallback simulado sem sondas → `memory: {}`; protocolo auditado por execução (RT-gate real;
  mediana/p95 vs statistics; timing NÃO roda sob tracemalloc — 5.3× de overhead evitado, medido;
  validate_pins pega +1B). **Achado fechado no ato**: idempotência sozinha aceitava
  decode-constante → RT de transformação virou **conteúdo-sob-transformação (multiset de linhas /
  valores posicionais) + idempotência 2ª geração**, com teste pinado do caso maligno.
- Suíte: **600 passed** (10 guardas novos do runner). Smoke real gravado em
  `evidencia-0.8/f1-smoke/D17a...jsonl` + `.tcf`.

## Aberto pra próxima sessão

- **Passo 2b (F2)**: controle minúsculo com o runner (single c/s header ×3, readers,
  README-propaganda, escaping/hex/fail-loud, 1 blob por dict, boundary 8192).
- Push quando o owner pedir (9 commits locais após o commit do F1).
