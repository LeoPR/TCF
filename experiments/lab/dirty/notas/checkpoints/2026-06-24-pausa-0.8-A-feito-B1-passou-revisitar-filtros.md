# Checkpoint 2026-06-24 — 0.8 workstream A feito, B1 passou, revisitar filtros [pausa]

**Tipo**: pausa explícita pra retomada. **Estado**: pré-1.0, 0.7.1 no PyPI; ciclo 0.8 em curso.

## O que foi feito nesta sessão (commits em `main`, sem push)

1. **Faxina** (`4401046`): dirty lab + docs organizados; 17 labs → `old/welded|refuted`; drift de
   docs de exemplo corrigido; snapshots marcados; `_wf_*.js` + `out_files/` destrackeados.
2. **A4** (`335b63d`): lazy view promovido `scripts/tcf_lazy/` → **`src/tcf/view.py`**
   (`from tcf import view`), shim de compat. Aditivo, zero regressão byte-canonical, 380 passed.
   + 4 tickets do ciclo 0.8 criados.
3. **Drift #TCF.6-default + A5** (`aa92642`): TCF-format.md (diagrama/Estado), STATUS (visão/foco),
   docs/README → 0.7. **A5** = `docs/reference/lazy-view.md` (Diátaxis, estável vs experimental).
   **Workstream A do 0.8 COMPLETO** (A1-A5).
4. **B1 cross-dict / H-GDICT** (`44b6f5e`, `d56b91e`, `1de6094`): caracterização completa em
   `experiments/lab/dirty/2026-06-21-gdict-caracterizacao/`. **Veredito: cross-dict PAGA no regime
   same-domain-refs** (colunas que referenciam o mesmo domínio: origem/destino, de/para, FK repetida,
   grafo from/to). Real: SNAP ca-GrQc **−19.3% textual** (2 colunas), OpenFlights −4.6%/−6.6%; lazy
   cross-col lê o dict 1× em vez de N×. Híbrido V2 (pool por grupo same-domain) captura e evita perdas.
   **Correção metodológica do owner: brotli NÃO é gate** (incompatível com lazy; nem sempre aplicado)
   → reforçada na memória [[gzip-e-compressao-externa-sao-intuicao-nao-parte-do-tcf]].
   Datasets reais em `Z:/tcf-data/external/` (provenance no lab).

## Decisão ABERTA (escopo/sequência do 0.8)

- **B2 entra no 0.8** (design #TCF.8 opt-in + híbrido V2; atrasa release) **OU**
- **fechar 0.8 = lazy + release agora**, cross-dict (#TCF.8) vira **0.9**.
- B2 toca `src/tcf` + formato → precisa de aprovação. **Pendente.**

## NOVO thread aberto pelo owner (2026-06-24) — revisitar FILTROS sob nova perspectiva

Owner resgatou os filtros/naturezas com um reframe: o pré-filtro tem **graus de entrega** entre
(a) **otimizar o formato** (encolher/limpar o valor → OBAT/HCC mais rápidos: CPF sem dígito
verificador/pontuação; IP→hex) e (b) **virar um "pré-dict"/bypass** pra tipos notórios (sim/não,
s/n, true/false, UF) onde o dict é **senso comum** → "viaja com TCF + versão", não no blob →
"compressão roubada"; o núcleo vira quase no-op. Conexão: lembrou disso por causa do **dict cross
(B1)**, só que **para tipos notórios** (dict universal/spec, não data-derived in-blob).

**Pedido do owner (ordem explícita)**: (1) ✅ registrar este momento; (2) análise crítica ANTES;
(3) depois revisar nossa pesquisa sobre isso; (4) avaliar em que momento focar nisso de novo.

**Retomar por aqui**: ver a planning-note `filtros-graus-de-entrega-2026-06-24.md` (análise crítica
+ consolidação da pesquisa + timing). Pesquisa existente a conectar: ADR-0015 (natures welded),
ADR-0027/F2 (nature-id no header, PARKED), natures_compiler F1/F1.5 (DSL+registry),
META-TYPE-ENCODERS, data-natures-taxonomy, dict-referencia-hipoteses (H-REF), cep-outer-dict-codebook
(H-CODEBOOK), B1 cross-dict.

## Invariantes mantidos
`src/tcf` intocado em todo o B1 e na faxina (só A4 tocou, aditivo+aprovado). Pré-1.0 ADR-0024.
Gate real-world + D1-D9=1523B/D17a=303B. Brotli = sinal, não gate (esp. p/ lazy).
