# Proveniência [probatório]

Fixtures reaproveitadas das peças 3/4/5 (mesma árvore), sintéticas/focadas na ESTRUTURA:
- **S4** `pessoa{nome} ⊃ telefones[{tel}]` · **S6** `pessoa{nome} ⊃ endereco{rua,cidade,geo{lat,lon}} + telefones[{tel}]`.

Valores fictícios (`leonardo` = exemplo do owner; tel `(41) 99999-9999` fictício; endereço sintético).
Valores como string. Determinístico. Só bytes de header + RT (nenhum dado real).
